@INITIALIZE_AMOUNT_FOR_FACES, ARG
#DIM DYNAMIC AMOUNT
#DIM DYNAMIC LCOUNT

SIF MAXBASE:ARG:便量 > 0
    RETURN

AMOUNT = 3000
FOR LCOUNT, 0, VARSIZE("TALENT")
	SIF !TALENT:ARG:LCOUNT
		CONTINUE
	SELECTCASE TALENTNAME:LCOUNT ; TODO:add case
		CASE "Coprophagia"
			最大値 *= 2
	ENDSELECT
NEXT

MAXBASE:ARG:便量 = AMOUNT

@INITIALIZE_AMOUNT_FOR_DEFECATE, ARG
#DIM DYNAMIC AMOUNT
#DIM DYNAMIC LCOUNT

AMOUNT = 3000
FOR LCOUNT, 0, VARSIZE("TALENT")
	SIF !TALENT:ARG:LCOUNT
		CONTINUE
	SELECTCASE TALENTNAME:LCOUNT ; TODO:add case
		CASE "Coprophilia"
			最大値 *= 3
            最大値 /= 2
	ENDSELECT
NEXT

MAXBASE:ARG:便意 = AMOUNT