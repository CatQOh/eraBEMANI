@INITIALIZE_AMOUNT_FOR_FACES, ARG
#DIM DYNAMIC AMOUNT
#DIM DYNAMIC LCOUNT

SIF MAXBASE:ARG:便量 > 0
    RETURN

AMOUNT = 3000
FOR LCOUNT, 0, VARSIZE("TALENT")
	SIF !TALENT:ARG:LCOUNT
		CONTINUE
	SELECTCASE TALENTNAME:LCOUNT ; TODO:add case
		;ommited coprophagia and coprophilia
		CASE "Coprophagia"
			AMOUNT *= 2
	ENDSELECT
NEXT

MAXBASE:ARG:便量 = AMOUNT

@INITIALIZE_AMOUNT_FOR_DEFECATE, ARG
#DIM DYNAMIC AMOUNT
#DIM DYNAMIC LCOUNT

AMOUNT = 3000
FOR LCOUNT, 0, VARSIZE("TALENT")
	SIF !TALENT:ARG:LCOUNT
		CONTINUE
	SELECTCASE TALENTNAME:LCOUNT ; TODO:add case
		;ommited coprophagia and coprophilia
		CASE "Coprophilia"
			AMOUNT *= 3
            AMOUNT /= 2
	ENDSELECT
NEXT

MAXBASE:ARG:便意 = AMOUNT
