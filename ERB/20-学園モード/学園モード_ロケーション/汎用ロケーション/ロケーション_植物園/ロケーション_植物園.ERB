@ロケーション条件_植物園
RETURN -1

@ロケーション可否_植物園
;時間帯などで使えない場合は理由を書く
SIF ターン == "夜"
	ロケーション可否 = 本日閉園

@バイト条件_植物園
;バイトの時間帯などを書く
バイト要項 = 朝～夕方の時間帯でシフト募集
;バイト可能かどうかの条件式
SIF GROUPMATCH(ターン, "朝", "昼", "夕")
	バイト可能 = 1
RETURN -1

@出現キャラ_植物園
#DIM DYNAMIC LCOUNT
#DIM DYNAMIC グループ数
#DIM DYNAMIC 始点
#DIM DYNAMIC 終点

;NOではなく登録番号で指定する
IF データベース参照中
	始点 = データベース参照中
	終点 = データベース参照中+1
ELSE
	IF 始点
		終点 = 始点
		始点 = 1
	ELSE
		始点 = RAND(1, CHARANUM)
		終点 = CHARANUM
	ENDIF
ENDIF

FOR LCOUNT, 始点, 終点
	;学校などに行ってるキャラは居ない
	SIF CSTR:LCOUNT:現在地 != "" && !データベース参照中
		CONTINUE
	;出現率:LCOUNT = {出現率(100分率)} で指定できる
	SELECTCASE CHARANAMEF(LCOUNT)
		{
		CASE "おはな", "マモニス", "ファウラ", "ボルテージ：マインツ", "マタン・カトルセ", "黄シグ", "嬬武器 烈風刀",
			 "ゴリラ", "スマイル", "シャラ", "ロビン", "クック", "シルビア", "ロキ", "ナズナ", "スズナ", "はなちゃん",
			 "ニコラシカ", "ヴィルヘルム", "らいむ", "マルクト", "フローラ", "ハーピア", "美結", "ヴォルフガング", "ちせ",
			 "璃音", "響香", "弦", "カイル", "セイナ", "真田 小幸村", "ユグドラシル", "ヨルド"
		}
			SIF 出現率:LCOUNT >= 0
				出現率:LCOUNT = 20
	ENDSELECT
NEXT

SIF 始点 == 1
	RETURN
SIF !データベース参照中
	RESTART
