@ロケーション条件_マジックアカデミーの寮
#DIM DYNAMIC LCOUNT

;QMAキャラ
FOR LCOUNT, 1, CHARANUM
	IF GROUPMATCH("クイズマジックアカデミー", CSTR:LCOUNT:登場作品, CSTR:LCOUNT:登場作品２, CSTR:LCOUNT:登場作品３)
		SELECTCASE 相性値(LCOUNT, PLAYER)
			;相性120%以上が条件
			CASE IS >= 120
				RETURN 1
		ENDSELECT
	ENDIF
NEXT

RETURN 0

@出現キャラ_マジックアカデミーの寮
#DIM DYNAMIC LCOUNT
#DIM DYNAMIC 始点
#DIM DYNAMIC 終点

;NOではなく登録番号で指定する
IF データベース参照中
	始点 = データベース参照中
	終点 = データベース参照中+1
ELSE
	始点 = 1
	終点 = CHARANUM
ENDIF

FOR LCOUNT, 始点, 終点
	;学校などに行ってるキャラは居ない
	SIF CSTR:LCOUNT:現在地 != "" && !データベース参照中
		CONTINUE
	;QMAキャラが出現
	SIF GROUPMATCH("クイズマジックアカデミー", CSTR:LCOUNT:登場作品, CSTR:LCOUNT:登場作品２, CSTR:LCOUNT:登場作品３)
		出現率:LCOUNT = 20
NEXT

	
