@SCHOOL_KOJO, 対象, シチュエーション
#DIM DYNAMIC LTARGET
#DIM DYNAMIC 対象
#DIMS DYNAMIC シチュエーション

LTARGET = TARGET
TARGET = 対象

IF OPTION:口上表示 && !CFLAG:対象:口上非表示
	TRYCCALLFORM SCHOOL_%CSVNAMERPS(NO:対象)%, シチュエーション
		CFLAG:対象:面識 = 1
	CATCH
		GOTO 口上なし
	ENDCATCH
ELSE
	$口上なし
	TRYCALLFORM SCHOOL_MESSAGE_%シチュエーション%
ENDIF

TARGET = LTARGET
RETURN RESULT
