@スキル効果, スキル名
#FUNCTIONS
#DIMS DYNAMIC スキル名

SELECTCASE スキル名
	CASE "格闘"
		RETURNF "格闘攻撃のダイスが増える レベルごとに1増加"
	CASE "剣術"
		RETURNF "刀剣武器によるダイス面がレベルごとに4増加"
	CASE "精密射撃（ライフル）"
		RETURNF "ライフルの命中率がレベルごとに10%増加 ダイス面がレベルごとに2増加"
	CASE "早撃ち（ライフル）"
		RETURNF "ライフルで追加射撃を行うことがある レベルごとに25%の確率"
	CASE "霊力"
		RETURNF "霊体へのダメージ2倍(未実装)"
	CASE "投擲"
		RETURNF "アイテム投擲時の命中率がレベルごとに10%増加 ダイス面がレベルごとに2増加"
	CASE "頑丈"
		RETURNF "レベルごとにダメージを1軽減 1未満にはならない"
	CASE "早撃ち（マシンガン）"
		RETURNF "マシンガンで追加射撃を行うことがある レベルごとに25%の確率"
	CASE "集中攻撃（格闘）"
		RETURNF "格闘攻撃の命中率が上がる レベルごとに10%増加"
	CASE "耐荷重量"
		RETURNF "持ち運べるアイテムの総重量が増加 レベルごとに5増加(未実装)"
	CASE "高燃費"
		RETURNF "満腹度が低下しやすい"
	CASE "料理"
		RETURNF "食材を拾ったときに調理して高品質な食料にする(未実装)"
	CASE "忍者"
		RETURNF "短剣によるダイス面がレベルごとに4増加"
	CASE "精密射撃（ハンドガン）"
		RETURNF "ハンドガンの命中率がレベルごとに10%増加 ダイス面がレベルごとに2増加"
	CASE "ギャンブラー"
		RETURNF "攻撃時のダイスロールで複数回のダイスから高い数字を優先する レベルごとに抽選回数+1(未実装)"
	CASE "幸運"
		RETURNF "アイテムの生成確率が上がる"
	CASE "不器用"
		RETURNF "武器使用時に耐久値が余分に減る(未実装)"
	CASE "逃げ足"
		RETURNF "敵から反対方向に移動するときに2マス移動できる(未実装)"
	CASE "自己治療"
		RETURNF "待機時の自然回復量増加(未実装)"
	CASE "社畜"
		RETURNF "自然回復量が低いが満腹度が低下しづらい"
	CASE "平和主義"
		RETURNF "自身の攻撃、敵の攻撃が50%の確率で無効化される(未実装)"
	CASE "犬飼い"
		RETURNF "犬属性の敵が中立になる"
	CASE "冷静"
		RETURNF "全ての攻撃の命中率がレベルごとに3%増加"
	CASE "サバイバル"
		RETURNF "満腹度消費量低下 自然回復力微増"
	CASE "ガンホルダー（ハンドガン）"
		RETURNF "ハンドガンの重さを1軽減(未実装)"
	CASE "フルスイング"
		RETURNF "鈍器によるダイス面がレベルごとに4増加"
	CASE "低燃費"
		RETURNF "満腹度消費量が低下"
	CASE "救急医療"
		RETURNF "隣接しているキャラを無差別に回復する(未実装)"
	CASE "薬学"
		RETURNF "ポーションが最初から鑑定済みになる(未実装)"
	CASE "早撃ち（ハンドガン）"
		RETURNF "ハンドガンで追加射撃を行うことがある レベルごとに25%の確率"
	CASE "絶対大丈夫だよ！"
		RETURNF "致命傷を20%の確率で無効化(未実装)"
	CASE "読書家"
		RETURNF "30%の確率で巻物を消費しない(未実装)"
	CASE "回避"
		RETURNF "攻撃を受けたときに敵の命中率をレベルごとに10%下げる"
	CASE "精密射撃（弓）"
		RETURNF "弓の命中率がレベルごとに10%増加 ダイス面がレベルごとに2増加"
	CASE "アーチャー"
		RETURNF "階段使用時にランダムな弓の使用回数が回復する レベルごとに+3(未実装)"
	CASE "きぐるみ"
		RETURNF "氷属性ダメージを半減(未実装)"
	CASE "反射"
		RETURNF "確率で遠距離攻撃を反射する レベルごとに10%(未実装)"
	CASE "精密射撃（重火器）"
		RETURNF "重火器の命中率がレベルごとに10%増加"
	CASE "過食"
		RETURNF "満腹度の最大値が倍"
	CASE "あついしぼう"
		RETURNF "炎属性と氷属性のダメージを半減(未実装)"
	CASE "ハッキング"
		RETURNF "機械属性の敵に近接攻撃すると30%の確率で中立にする(未実装)"
	CASE "千里眼"
		RETURNF "階段の場所が分かる(未実装)"
	CASE "猫語話者"
		RETURNF "猫属性の敵が中立になる"
	CASE "倹約"
		RETURNF "アイテムを安く買える(未実装)"
	CASE "耐冷"
		RETURNF "氷属性のダメージを受けてもポーションが割れない(未実装)"
	CASE "脆弱"
		RETURNF "受けるダメージがレベルごとに1増加する"
	CASE "お狐同盟"
		RETURNF "狐属性の敵が中立になる"
	CASE "ガンスミス"
		RETURNF "階段使用時にランダムな銃の使用回数が回復する レベルごとに+3(未実装)"
	CASE "浪費"
		RETURNF "アイテムの値段が高くなる(未実装)"
	CASE "監視者"
		RETURNF "敵の場所が分かる(未実装)"
	CASE "浮遊"
		RETURNF "罠を無効化する(未実装)"
	CASE "武器使用不可"
		RETURNF "武器、投擲が使用できない"
	CASE "断罪"
		RETURNF "中立キャラを攻撃したことがあると受けるダメージが倍増"
	CASE "九生"
		RETURNF "倒れても8回まで復活する(未実装)"
	CASE "スパイ"
		RETURNF "敵が稀に中立になる"
	CASE "植生学"
		RETURNF "薬草が鑑定済みになる(未実装)"
	CASE "身代わりの術"
		RETURNF "10%の確率で攻撃を無効化する"

	;魔法系
	CASE "魅了魔法"
		RETURNF "当たるとダメージを受けるまで味方にする魔法を使える(未実装)"
	CASE "暗黒魔法"
		RETURNF "当たると視界が無くなる魔法を使える(未実装)"
	CASE "毒魔法"
		RETURNF "当たると継続ダメージを与える魔法を使える(未実装)"
	CASE "封印魔法"
		RETURNF "敵の魔法を封じる魔法攻撃を使える(未実装)"
	CASE "炎魔法"
		RETURNF "敵を火傷させる魔法攻撃を使える(未実装)"
	CASE "水魔法"
		RETURNF "敵を後退させる魔法攻撃を使える(未実装)"
	CASE "光魔法"
		RETURNF "悪魔に効果的な強力な魔法攻撃を使える(未実装)"
ENDSELECT

RETURNF ""
