@TRAIN_COMMENT, コマンド名
#FUNCTIONS
#DIMS DYNAMIC コマンド名
#DIMS DYNAMIC ツールチップ

SELECTCASE コマンド名
	CASE "愛撫"
		IF LANGUAGE == "ENG"
			ツールチップ = Caressing %CALLNAME:TARGET%`s body
		ELSE
			ツールチップ = %CALLNAME:TARGET%の体を撫で回します
		ENDIF
	CASE "クンニ"
		IF LANGUAGE == "ENG"
			ツールチップ = Licking %CALLNAME:TARGET%`s pussy
		ELSE
			ツールチップ = %CALLNAME:TARGET%の女性器を舐め解します
		ENDIF
	CASE "フェラする"
		IF LANGUAGE == "ENG"
			ツールチップ = Serve %CALLNAME:TARGET%`s penis with your mouth
		ELSE
			ツールチップ = %CALLNAME:TARGET%の男性器に口で奉仕します
		ENDIF
	CASE "指挿入れ"
		IF LANGUAGE == "ENG"
			ツールチップ = Insert your finger into %CALLNAME:TARGET%`s pussy
		ELSE
			ツールチップ = %CALLNAME:TARGET%の女性器に指を挿入します
		ENDIF
	CASE "アナル舐め"
		IF LANGUAGE == "ENG"
			ツールチップ = Lick %CALLNAME:TARGET%`s anus
		ELSE
			ツールチップ = %CALLNAME:TARGET%の肛門を舐め解します
		ENDIF
	CASE "アナル愛撫"
		IF LANGUAGE == "ENG"
			ツールチップ = Play with %CALLNAME:TARGET%`s anus with own fingers
		ELSE
			ツールチップ = %CALLNAME:TARGET%の肛門を指で弄ります
		ENDIF
	CASE "胸揉み"
		IF LANGUAGE == "ENG"
			ツールチップ = Massage %CALLNAME:TARGET%`s breasts
		ELSE
			ツールチップ = %CALLNAME:TARGET%の乳房を揉みしだきます
		ENDIF
	CASE "乳首責め"
		IF LANGUAGE == "ENG"
			ツールチップ = Caress %CALLNAME:TARGET%`s nipples
		ELSE
			ツールチップ = %CALLNAME:TARGET%の乳首を重点的に愛撫します
		ENDIF
	CASE "胸愛撫"
		IF LANGUAGE == "ENG"
			ツールチップ =Gently caress  %CALLNAME:TARGET%`s chest
		ELSE
			ツールチップ = %CALLNAME:TARGET%の胸部を優しく撫で回します
		ENDIF
	CASE "秘貝開帳"
		IF LANGUAGE == "ENG"
			ツールチップ = Order %CALLNAME:TARGET% to spread his or her genitals
		ELSE
			ツールチップ = %CALLNAME:TARGET%に、自分で女性器を拡げるように命じます
		ENDIF
	CASE "胸自慰"
		IF LANGUAGE == "ENG"
			ツールチップ = Order %CALLNAME:TARGET% to masturbate while playing own breasts.
		ELSE
			ツールチップ = %CALLNAME:TARGET%に、胸を弄りながらの自慰を命じます
		ENDIF
	CASE "自慰"
		IF LANGUAGE == "ENG"
			ツールチップ = Order %CALLNAME:TARGET% to masturbate
		ELSE
			ツールチップ = %CALLNAME:TARGET%に、好きなように自慰をするように命じます
		ENDIF
	CASE "足扱きする"
		IF LANGUAGE == "ENG"
			ツールチップ = Step on or handle %CALLNAME:TARGET%`s penis with your feet
		ELSE
			ツールチップ = %CALLNAME:TARGET%の男性器を足で踏んだり扱いたりします
		ENDIF
	CASE "下着扱きする"
		IF LANGUAGE == "ENG"
			ツールチップ = Order %CALLNAME:PLAYER% to treat  %CALLNAME:TARGET%`s %I18N("CSTR:PLAYER:下半身下着", "TRAIN")%
		ELSE
			ツールチップ = %CALLNAME:PLAYER%が穿いてい\@ TEQUIP:PLAYER:下半身下着 ? る # た \@%CSTR:PLAYER:下半身下着%で、%CALLNAME:TARGET%の男性器を扱きます
		ENDIF
	CASE "キスする"
		IF LANGUAGE == "ENG"
			ツールチップ = Kiss %CALLNAME:TARGET%. may be refused in some cases
		ELSE
			ツールチップ = %CALLNAME:TARGET%とキスをします。場合によっては拒否される場合があります
		ENDIF
	CASE "何もしない"
		IF LANGUAGE == "ENG"
			ツールチップ = Leave %CALLNAME:TARGET% alone for awhile
		ELSE
			ツールチップ = 何もせずに%CALLNAME:TARGET%を放置します
		ENDIF
	CASE "会話"
		IF LANGUAGE == "ENG"
			ツールチップ = Have a casual chat with %CALLNAME:TARGET%
		ELSE
			ツールチップ = %CALLNAME:TARGET%と他愛もない会話をします
		ENDIF
	CASE "プレゼント"
		IF LANGUAGE == "ENG"
			ツールチップ = Give a gift to %CALLNAME:TARGET%. Some things require money
		ELSE
			ツールチップ = %CALLNAME:TARGET%に贈り物をします。物によってはお金が必要になります
		ENDIF
	CASE "スキンシップ"
		IF LANGUAGE == "ENG"
			ツールチップ = Play with %CALLNAME:TARGET%
		ELSE
			ツールチップ = %CALLNAME:TARGET%とじゃれ合います
		ENDIF
	CASE "まったりする"
		IF LANGUAGE == "ENG"
			ツールチップ = Chill out with %CALLNAME:TARGET% together for awhile
		ELSE
			ツールチップ = %CALLNAME:TARGET%とまったりします
		ENDIF
	CASE "家事"
		IF LANGUAGE == "ENG"
			ツールチップ = Clean up %CALLNAME:TARGET% and house chores
		ELSE
			ツールチップ = %CALLNAME:TARGET%と家の雑事を片付けます
		ENDIF
	CASE "歌の練習"
		IF LANGUAGE == "ENG"
			ツールチップ = Train %CALLNAME:TARGET%`s singing ability
		ELSE
			ツールチップ = %CALLNAME:TARGET%の歌唱力を鍛えます
		ENDIF
	CASE "罵倒"
		IF LANGUAGE == "ENG"
			ツールチップ = Hurl all sorts of insults at %CALLNAME:TARGET%
		ELSE
			ツールチップ = %CALLNAME:TARGET%にありとあらゆる罵詈雑言をぶつけます
		ENDIF
	CASE "ビデオ鑑賞"
		IF LANGUAGE == "ENG"
			ツールチップ = Watch videos with %CALLNAME:TARGET% that taken in the past together.
		ELSE
			ツールチップ = 過去に撮影した%CALLNAME:TARGET%とのビデオを一緒に鑑賞します
		ENDIF
	CASE "吸血する"
		IF LANGUAGE == "ENG"
			ツールチップ = Suck some blood from %CALLNAME:TARGET%
		ELSE
			ツールチップ = %CALLNAME:TARGET%から血を吸います
		ENDIF
	CASE "吸血させる"
		IF LANGUAGE == "ENG"
			ツールチップ = Order %CALLNAME:TARGET% to suck blood. %CALLNAME:TARGET% will recover, but it can only be done once during the training.
		ELSE
			ツールチップ = %CALLNAME:TARGET%に血を吸わせます。%CALLNAME:TARGET%は回復しますが、その調教中に一度しか行なえません
		ENDIF
	CASE "お酒を飲ませる"
		IF LANGUAGE == "ENG"
			ツールチップ = Serve a drink for %CALLNAME:TARGET%
		ELSE
			ツールチップ = %CALLNAME:TARGET%にお酒を飲ませます
		ENDIF
	CASE "ローター"
		IF LANGUAGE == "ENG"
			IF TALENT:去勢済み
				ツールチップ = Use the rotor for torture %CALLNAME:TARGET%`s castration marks
			ELSE
				ツールチップ = use rotor to stimulate %CALLNAME:TARGET%`'s \@ PENIS(TARGET) ? penis # clitoris \@
			ENDIF 
		ELSE
			IF TALENT:去勢済み
				ツールチップ = %CALLNAME:TARGET%の去勢痕を中心にローターで責め立てます
			ELSE
				ツールチップ = %CALLNAME:TARGET%の\@ PENIS(TARGET) ? 陰茎 # 陰核 \@を中心にローターで責め立てます
			ENDIF
		ENDIF
	CASE "電気按摩"
		IF LANGUAGE == "ENG"
			IF TALENT:去勢済み
				ツールチップ = Use electric massager for torture %CALLNAME:TARGET%`s castration marks
			ELSE
				ツールチップ = use electric massager to stimulate %CALLNAME:TARGET%`'s \@ PENIS(TARGET) ? penis # clitoris \@
			ENDIF 
		ELSE
			IF TALENT:去勢済み
				ツールチップ = %CALLNAME:TARGET%の去勢痕を中心に電気按摩で責め立てます
			ELSE
				ツールチップ = %CALLNAME:TARGET%の\@ PENIS(TARGET) ? 陰茎 # 陰核 \@を中心に電気按摩で責め立てます
			ENDIF
		ENDIF
	CASE "クリキャップ"
		IF LANGUAGE == "ENG"
			IF TEQUIP:クリキャップ
				ツールチップ = Remove the vibrating clip cap from %CALLNAME:TARGET%`s clitoris
			ELSE
				ツールチップ = Attach a vibrating clip cap to %CALLNAME:TARGET%`s clitoris
			ENDIF
		ELSE
			IF TEQUIP:クリキャップ
				ツールチップ = %CALLNAME:TARGET%の陰核から振動機能付きクリキャップを取り外します
			ELSE
				ツールチップ = %CALLNAME:TARGET%の陰核に振動機能付きクリキャップを取り付けます
			ENDIF
		ENDIF
	CASE "オナホール"
		IF LANGUAGE == "ENG"
			IF TEQUIP:オナホール
				ツールチップ = Remove the masturbator from the %CALLNAME:TARGET%`s penis
			ELSE
				ツールチップ = Put %CALLNAME:TARGET%`s penis into a masturbator
			ENDIF
		ELSE
			IF TEQUIP:オナホール
				ツールチップ = %CALLNAME:TARGET%の陰茎からオナホールを取り外します
			ELSE
				ツールチップ = %CALLNAME:TARGET%の陰茎にオナホールを被せます
			ENDIF
		ENDIF
	CASE "バイブ"
		IF LANGUAGE == "ENG"
			IF TEQUIP:バイブ
				ツールチップ = Pull out the vibrator from %CALLNAME:TARGET%`s vagina
			ELSE
				ツールチップ = Insert the vibrator into %CALLNAME:TARGET%`s vagina
			ENDIF
		ELSE
			IF TEQUIP:バイブ
				ツールチップ = %CALLNAME:TARGET%の膣からバイブを引き抜きます
			ELSE
				ツールチップ = %CALLNAME:TARGET%の膣にバイブを挿入します
			ENDIF
		ENDIF
	CASE "アナルバイブ"
		IF LANGUAGE == "ENG"
			IF TEQUIP:アナルバイブ
				ツールチップ = Pull out the vibrator from %CALLNAME:TARGET%`s anus
			ELSE
				ツールチップ = Insert the vibrator into %CALLNAME:TARGET%`s anus
			ENDIF
		ELSE
			IF TEQUIP:アナルバイブ
				ツールチップ = %CALLNAME:TARGET%の肛門からバイブを引き抜きます
			ELSE
				ツールチップ = %CALLNAME:TARGET%の肛門にバイブを挿入します
			ENDIF
		ENDIF
	CASE "アナルビーズ"
		IF LANGUAGE == "ENG"
			IF TEQUIP:アナルビーズ
				ツールチップ = Pull out the anal beads from the anus of %CALLNAME:TARGET%
			ELSE
				ツールチップ = Let the anal beads be swallowed into  %CALLNAME:TARGET%`s anus
			ENDIF
		ELSE
			IF TEQUIP:アナルビーズ
				ツールチップ = %CALLNAME:TARGET%の肛門からアナルビーズを引き抜きます
			ELSE
				ツールチップ = %CALLNAME:TARGET%の肛門にアナルビーズを飲み込ませていきます
			ENDIF
		ENDIF
	CASE "ニプルキャップ"
		IF LANGUAGE == "ENG"
			IF TEQUIP:ニプルキャップ
				ツールチップ = Remove the rotor from the %CALLNAME:TARGET% nipple
			ELSE
				ツールチップ = Attach the rotor to the %CALLNAME:TARGET% nipple
			ENDIF
		ELSE
			IF TEQUIP:ニプルキャップ
				ツールチップ = %CALLNAME:TARGET%の乳首からローターを取り外します
			ELSE
				ツールチップ = %CALLNAME:TARGET%の乳首にローターを取り付けます
			ENDIF
		ENDIF
	CASE "乳首ローター"
		IF LANGUAGE == "ENG"
			ツールチップ = Massage %CALLNAME:TARGET%`s nipples with a rotor
		ELSE
			ツールチップ = %CALLNAME:TARGET%の乳首をローターで責め立てます
		ENDIF
	CASE "搾乳器"
		IF LANGUAGE == "ENG"
			IF TEQUIP:搾乳器
				ツールチップ = Remove breast pump from %CALLNAME:TARGET%`s breasts
			ELSE
				ツールチップ = Attach the breast milker to %CALLNAME:TARGET%`s breast.
			ENDIF
		ELSE
			IF TEQUIP:搾乳器
				ツールチップ = %CALLNAME:TARGET%の乳房から搾乳器を取り外します
			ELSE
				ツールチップ = 母乳を搾り取る器具を%CALLNAME:TARGET%の乳房に取り付けます
			ENDIF
		ENDIF
	CASE "搾精コンドーム"
		IF LANGUAGE == "ENG"
			IF TEQUIP:搾精コンドーム
				ツールチップ = Remove the a cum filled collectors condom from %CALLNAME:TARGET%`s penis
			ELSE
				ツールチップ = Cover %CALLNAME:TARGET%'s penis with a condom for semen extraction.
			ENDIF
		ELSE
			IF TEQUIP:搾精コンドーム
				ツールチップ = %CALLNAME:TARGET%のペニスから搾精用のコンドームを取り外します
			ELSE
				ツールチップ = %CALLNAME:TARGET%のペニスに精液搾取用のコンドームを被せます
			ENDIF
		ENDIF
	CASE "コンドーム"
		IF LANGUAGE == "ENG"
			ツールチップ = Wear a rubber as contraceptive
		ELSE
			ツールチップ = 避妊具を付けたり外したりします
		ENDIF
	CASE "正常位"
		IF LANGUAGE == "ENG"
			ツールチップ = Insert the \@ PENIS(PLAYER) ? Penis # Strapon \@ into %CALLNAME:TARGET%`s vagina from the front in missionary
		ELSE
			ツールチップ = 正面から%CALLNAME:TARGET%の女性器に\@ PENIS(PLAYER) ? ペニス # ペニスバンド \@を挿入します
		ENDIF
	CASE "後背位"
		IF LANGUAGE == "ENG"
			ツールチップ = Insert the \@ PENIS(PLAYER) ? Penis # Strapon \@ into %CALLNAME:TARGET%`s vagina from the back 
		ELSE
			ツールチップ = 後ろから%CALLNAME:TARGET%の女性器に\@ PENIS(PLAYER) ? ペニス # ペニスバンド \@を挿入します
		ENDIF
	CASE "騎乗位"
		IF LANGUAGE == "ENG"
			ツールチップ = %CALLNAME:TARGET% will be ordered to swallow %CALLNAME:PLAYER%`s penis with own genitals.
		ELSE
			ツールチップ = %CALLNAME:TARGET%に、%CALLNAME:PLAYER%のペニスを自分から女性器で咥え込むように命じます
		ENDIF
	CASE "対面座位"
		IF LANGUAGE == "ENG"
			ツールチップ = Have sex while hugging %CALLNAME:TARGET%
		ELSE
			ツールチップ = %CALLNAME:TARGET%と抱き合ったまま性交を行います
		ENDIF
	CASE "背面座位"
		IF LANGUAGE == "ENG"
			ツールチップ = Hugging %CALLNAME:TARGET% from behind and having sex
		ELSE
			ツールチップ = 後ろから%CALLNAME:TARGET%を抱き寄せて性交を行います
		ENDIF
	CASE "正常位アナル"
		IF LANGUAGE == "ENG"
			ツールチップ = Insert the \@ PENIS(PLAYER) ? penis # strapon \@ into %CALLNAME:TARGET%`s asshole from the front
		ELSE 
			ツールチップ = 正面から%CALLNAME:TARGET%の尻穴に\@ PENIS(PLAYER) ? ペニス # ペニスバンド \@を挿入します
		ENDIF
	CASE "後背位アナル"
		IF LANGUAGE == "ENG"
			ツールチップ = Insert the \@ PENIS(PLAYER) ? penis # strapon \@ into %CALLNAME:TARGET%`s asshole from behind
		ELSE
			ツールチップ = 後ろから%CALLNAME:TARGET%の尻穴に\@ PENIS(PLAYER) ? ペニス # ペニスバンド \@を挿入します
		ENDIF
	CASE "騎乗位アナル"
		IF LANGUAGE == "ENG"
			ツールチップ = order %CALLNAME:TARGET% to ride %CALLNAME:PLAYER%`s \@ PENIS(PLAYER) ? penis # strapon \@ from top
		ELSE
			ツールチップ = %CALLNAME:TARGET%に、%CALLNAME:PLAYER%のペニスを自分から尻穴で咥え込むように命じます
		ENDIF
	CASE "対面座位アナル"
		IF LANGUAGE == "ENG"
			ツールチップ = Insert the \@ PENIS(PLAYER) ? penis # strapon \@ into the asshole while hugging %CALLNAME:TARGET%
		ELSE
			ツールチップ = %CALLNAME:TARGET%と抱き合ったまま尻穴にペニスを挿入します
		ENDIF
	CASE "背面座位アナル"
		IF LANGUAGE == "ENG"
			ツールチップ = Hold %CALLNAME:TARGET% from behind and insert penis into %CALLNAME:TARGET%`s asshole
		ELSE
			ツールチップ = 後ろから%CALLNAME:TARGET%を抱き寄せて尻穴にペニスを挿入します
		ENDIF
	CASE "下着ずらし挿入"
		IF LANGUAGE == "ENG"
			ツールチップ = moving down %CALLNAME:TARGET%`s \@ TEQUIP:下半身下着 ? wearing %CSTR:下半身下着% # wearing %CSTR:全身上着% \@nd insert the penis into her vagina
		ELSE
			ツールチップ = %CALLNAME:TARGET%が\@ TEQUIP:下半身下着 ? 穿いている%CSTR:下半身下着% # 着ている%CSTR:全身上着% \@をずらして女性器にペニスを挿入します
		ENDIF
	CASE "下着ずらし挿入アナル"
		IF LANGUAGE == "ENG"
			ツールチップ = %CALLNAME:TARGET%が\@ TEQUIP:下半身下着 ? 穿いている%CSTR:下半身下着% # 着ている%CSTR:全身上着% \@をずらして尻穴にペニスを挿入します
		ELSE
			ツールチップ = %CALLNAME:TARGET%が\@ TEQUIP:下半身下着 ? 穿いている%CSTR:下半身下着% # 着ている%CSTR:全身上着% \@をずらして尻穴にペニスを挿入します
		ENDIF
	CASE "正常位させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に\@ PLAYER == TMASTER ? 自分 # %CALLNAME:ASSI% \@を抱くようにと命じます
		ENDIF
	CASE "後背位させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に\@ PLAYER == TMASTER ? 自分 # %CALLNAME:ASSI% \@を後ろから犯すようにと命じます
		ENDIF
	CASE "正常位アナルさせる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のペニスを\@ PLAYER == TMASTER ? 自分 # %CALLNAME:ASSI% \@の尻穴に挿入するようにと命じます
		ENDIF
	CASE "後背位アナルさせる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に\@ PLAYER == TMASTER ? 自分 # %CALLNAME:ASSI% \@の尻穴を後ろから犯すようにと命じます
		ENDIF
	CASE "逆レイプ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のペニスを\@ VAGINA(PLAYER) ? 膣 # 尻穴 \@で強引に咥え込みます
		ENDIF
	CASE "スライム姦"
		IF LANGUAGE == "ENG"
		ELSE
		IF ISSLIME(TARGET)
			ツールチップ = 同じスライム同士で混ざり合うようにして%CALLNAME:TARGET%を犯します
		ELSE
			ツールチップ = スライムの体を活かして%CALLNAME:TARGET%の全身の穴という穴を犯します
		ENDIF
		ENDIF
	CASE "スライム姦させる"
		IF LANGUAGE == "ENG"
		ELSE
		IF ISSLIME(TMASTER) && PLAYER == TMASTER
			ツールチップ = %CALLNAME:TARGET%に自身の形を捨てて自分と混ざり合うように命じます
		ELSEIF ASSI > 0 && ISSLIME(ASSI) && PLAYER != TMASTER
			ツールチップ = %CALLNAME:TARGET%に自身の形を捨てて%CALLNAME:ASSI%と混ざり合うように命じます
		ELSE
			ツールチップ = %CALLNAME:TARGET%に自身の形を捨てて\@ PLAYER == TMASTER ? 自分 # %CALLNAME:ASSI% \@を犯すように命じます
		ENDIF
		ENDIF
	CASE "手淫"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に手で%CALLNAME:PLAYER%のペニスを扱くように命じます
		ENDIF
	CASE "フェラチオ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に口で%CALLNAME:PLAYER%のペニスに奉仕するように命じます
		ENDIF
	CASE "パイズリ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の乳房にペニスを挟んで扱き上げます
		ENDIF
	CASE "素股"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の陰部に%CALLNAME:PLAYER%のペニスを擦りつけます
		ENDIF
	CASE "泡踊り"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 泡にまみれた%CALLNAME:TARGET%の全身で奉仕させます
		ENDIF
	CASE "足扱き"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の足で%CALLNAME:PLAYER%のペニスを扱くように命じます
		ENDIF
	CASE "足を舐めさせる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に%CALLNAME:PLAYER%の足を舐めさせ権威を示します
		ENDIF
	CASE "縦パイズリ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 正面から%CALLNAME:TARGET%の乳房の間にペニスを埋めます
		ENDIF
	CASE "パイズリフェラ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の乳房でペニスを挟みながら同時に口で奉仕するように命じます
		ENDIF
	CASE "母乳飲み"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の母乳を飲みます
		ENDIF
	CASE "兜合わせ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と%CALLNAME:PLAYER%のペニスを擦り合わせます
		ENDIF
	CASE "下着扱き"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%が穿いてい\@ TEQUIP:下半身下着 ? る # た \@%CSTR:下半身下着%で%CALLNAME:PLAYER%のペニスを扱かせます
		ENDIF
	CASE "オナホ扱き"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%にオナホで%CALLNAME:PLAYER%のペニスを扱かせます
		ENDIF
	CASE "スパンキング"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の尻を何度も叩きます
		ENDIF
	CASE "乳スパンキング"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の胸を軽く叩きます
		ENDIF
	CASE "鞭"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%を鞭で殴打します
		ENDIF
	CASE "蝋燭"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の体に熱した蝋を垂らします
		ENDIF
	CASE "針"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の陰部を針で責め立てます
		ENDIF
	CASE "アイマスク"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:アイマスク
			ツールチップ = %CALLNAME:TARGET%からアイマスクを外します
		ELSE
			ツールチップ = %CALLNAME:TARGET%の視界をアイマスクで覆います
		ENDIF
		ENDIF
	CASE "縄"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:縄
			ツールチップ = %CALLNAME:TARGET%の縄を解きます
		ELSE
			ツールチップ = %CALLNAME:TARGET%を縄で縛り付けます
		ENDIF
		ENDIF
	CASE "ボールギャグ"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:ボールギャグ
			ツールチップ = %CALLNAME:TARGET%から轡を外します
		ELSE
			ツールチップ = %CALLNAME:TARGET%の口に轡を嵌めて喋られないようにします
		ENDIF
		ENDIF
	CASE "腹パン"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の腹部を思い切り殴ります
		ENDIF
	CASE "踏む"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のいろんな部位を踏みます
		ENDIF
	CASE "裸締め"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%を後ろから羽交い締めにします。体力次第で気絶する可能性があります
		ENDIF
	CASE "首を絞める"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 死なない程度に%CALLNAME:TARGET%の首を締めます
		ENDIF
	CASE "ビンタ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の頬を平手打ちします
		ENDIF
	CASE "異物挿入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の\@ VAGINA(TARGET) ? 女性器、もしくは #  \@肛門に様々な異物を挿入します
		ENDIF
	CASE "異物挿入解除"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の\@ VAGINA(TARGET) ? 女性器、もしくは #  \@肛門に挿入した異物を引き抜きます
		ENDIF
	CASE "クンニさせる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に、%CALLNAME:PLAYER%の女性器に口で奉仕するように命じます
		ENDIF
	CASE "貝合わせ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と互いの女性器を擦り付け合います
		ENDIF
	CASE "助手を犯させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に%CALLNAME:ASSI%を犯させます
		ENDIF
	CASE "顔面騎乗する"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の顔面に跨って無理やり口で奉仕させます
		ENDIF
	CASE "ダブルフェラ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と%CALLNAME:PLAYER%の二人同時のフェラチオを楽しみます
		ENDIF
	CASE "ダブルパイズリ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と%CALLNAME:PLAYER%の二人同時のパイズリを楽しみます
		ENDIF
	CASE "乳の揉み合い"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と互いの乳房を揉み合います
		ENDIF
	CASE "イラマチオ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の喉奥までペニスを突き入れます
		ENDIF
	CASE "フィストファック"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の女性器に拳を挿入します
		ENDIF
	CASE "アナルフィスト"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のアナルに拳を挿入します
		ENDIF
	CASE "両穴フィスト"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の女性器とアナルに拳を挿入します
		ENDIF
	CASE "放尿"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%にその場で放尿させます
		ENDIF
	CASE "アナルを舐めさせる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に%CALLNAME:PLAYER%の肛門を舐めさせます
		ENDIF
	CASE "浣腸器"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:浣腸器
			ツールチップ = %CALLNAME:TARGET%のアナルプラグを外して排便させます
		ELSE
			ツールチップ = %CALLNAME:TARGET%の腸内に浣腸液を流し込んで栓をします
		ENDIF
		ENDIF
	CASE "空気浣腸"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の尻穴に空気を流し込みます
		ENDIF
	CASE "拡張バルーン"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:拡張バルーン
			ツールチップ = %CALLNAME:TARGET%の尻穴を拡げるバルーンを萎めます
		ELSE
			ツールチップ = %CALLNAME:TARGET%の尻穴をバルーンで拡張します
		ENDIF
		ENDIF
	CASE "アナル電極"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:アナル電極
			ツールチップ = %CALLNAME:TARGET%のアナルから電極付きプラグを引き抜きます
		ELSE
			ツールチップ = 電気が流れるプラグを%CALLNAME:TARGET%のアナルに挿入します
		ENDIF
		ENDIF
	CASE "乳房電極"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:乳房電極
			ツールチップ = %CALLNAME:TARGET%の乳首から電極付きニップレスを取り外します
		ELSE
			ツールチップ = 電気が流れるニップレスを%CALLNAME:TARGET%の乳首に取り付けます
		ENDIF
		ENDIF
	CASE "乳ファック"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の胸部で強引にペニスを扱き上げます
		ENDIF
	CASE "触手召喚"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:触手
			ツールチップ = 触手を送り返します
		ELSE
			ツールチップ = 触手を召喚します
		ENDIF
		ENDIF
	CASE "触手挿入"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:バイブ
			ツールチップ = %CALLNAME:TARGET%の膣内から触手を引き抜きます
		ELSE
			ツールチップ = %CALLNAME:TARGET%の膣内を触手で蹂躙します
		ENDIF
		ENDIF
	CASE "アナル触手"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:アナルバイブ
			ツールチップ = %CALLNAME:TARGET%の尻穴から触手を引き抜きます
		ELSE
			ツールチップ = %CALLNAME:TARGET%の腸内を触手で蹂躙します
		ENDIF
		ENDIF
	CASE "触手クリ責め"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:クリキャップ
			ツールチップ = %CALLNAME:TARGET%の陰核に吸い付く触手にやめるよう指示します
		ELSE
			ツールチップ = 触手に%CALLNAME:TARGET%の陰核を弄り倒すように指示します
		ENDIF
		ENDIF
	CASE "触手乳首責め"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:ニプルキャップ
			ツールチップ = %CALLNAME:TARGET%の乳首に吸い付く触手にやめるよう指示します
		ELSE
			ツールチップ = 触手に%CALLNAME:TARGET%の乳首を弄り倒すように指示します
		ENDIF
		ENDIF
	CASE "触手搾乳"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:搾乳器
			ツールチップ = %CALLNAME:TARGET%の乳搾りを終えます
		ELSE
			ツールチップ = 触手で%CALLNAME:TARGET%の母乳を搾ります
		ENDIF
		ENDIF
	CASE "触手緊縛"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:縄
			ツールチップ = %CALLNAME:TARGET%を縛る触手を解きます
		ELSE
			ツールチップ = 触手で%CALLNAME:TARGET%の体を拘束します
		ENDIF
		ENDIF
	CASE "触手浣腸"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:浣腸器
			ツールチップ = %CALLNAME:TARGET%の尻穴から触手を引き抜いて排便させます
		ELSE
			ツールチップ = 触手から直に%CALLNAME:TARGET%の腸内へと浣腸液を流し込みます
		ENDIF
		ENDIF
	CASE "触手口辱"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:触手口辱
			ツールチップ = %CALLNAME:TARGET%の口から触手を引き抜きます
		ELSE
			ツールチップ = %CALLNAME:TARGET%の口を触手で塞ぎます
		ENDIF
		ENDIF
	CASE "触手ペニス責め"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:オナホール
			ツールチップ = %CALLNAME:TARGET%のペニスに吸い付く触手にやめるよう指示します
		ELSE
			ツールチップ = 触手で%CALLNAME:TARGET%のペニスを咥え込みます
		ENDIF
		ENDIF
	CASE "触手搾精"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:搾精コンドーム
			ツールチップ = %CALLNAME:TARGET%のペニスに吸い付く触手にやめるよう指示します
		ELSE
			ツールチップ = 触手で%CALLNAME:TARGET%の精液を搾り取ります
		ENDIF
		ENDIF
	CASE "ローション"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の体にローションを塗りたくります
		ENDIF
	CASE "媚薬"
		IF LANGUAGE == "ENG"
			ツールチップ = Give an aphrodisiac to %CALLNAME:TARGET%
		ELSE
			ツールチップ = %CALLNAME:TARGET%に媚薬を飲ませます
		ENDIF
	CASE "魅惑の粘液"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の体に甘い香りのする謎の粘液を塗りつけます
		ENDIF
	CASE "利尿剤"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に利尿剤を飲ませます
		ENDIF
	CASE "ビデオカメラ"
		IF LANGUAGE == "ENG"
		ELSE
			IF TEQUIP:ビデオカメラ
				ツールチップ = 撮影を終えます
			ELSE
				ツールチップ = %CALLNAME:TARGET%の痴態を撮影します
			ENDIF
		ENDIF
	CASE "野外プレイ"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:野外プレイ
			ツールチップ = 家に帰ります
		ELSE
			ツールチップ = %CALLNAME:TARGET%に首輪を付けて外に連れ出します
		ENDIF
		ENDIF
	CASE "粘液ぶっかけ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 溜め込んでいた保管済み粘液を%CALLNAME:TARGET%に浴びせます
		ENDIF
	CASE "助手を犯す"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の目の前で%CALLNAME:ASSI%を犯します
		ENDIF
	CASE "羞恥プレイ"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:羞恥プレイ
			ツールチップ = 普通の部屋に戻ります
		ELSE
			ツールチップ = %CALLNAME:TARGET%を大きな鏡のある部屋へと連れて行きます
		ENDIF
		ENDIF
	CASE "お風呂場プレイ"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:お風呂場
			ツールチップ = お風呂を上がります
		ELSE
			ツールチップ = %CALLNAME:TARGET%をお風呂場へと連れて行きます
		ENDIF
		ENDIF
	CASE "シャワー"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:シャワー
			ツールチップ = シャワーを止めます
		ELSE
			ツールチップ = %CALLNAME:TARGET%にシャワーを浴びせます
		ENDIF
		ENDIF
	CASE "新妻プレイ"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:エプロン
			ツールチップ = 新婚ごっこを終えます
		ELSE
			ツールチップ = %CALLNAME:TARGET%にエプロンを着せます
		ENDIF
		ENDIF
	CASE "写真撮影"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 直前の行為を撮影します
		ENDIF
	CASE "強精神薬"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に絶頂を止める薬を飲ませます
		ENDIF
	CASE "コンドーム精飲（奴隷）"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 避妊具に溜まった精液を%CALLNAME:TARGET%に飲ませます
		ENDIF
	CASE "コンドーム精飲（助手）"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 避妊具に溜まった精液を%CALLNAME:ASSI%に飲ませます
		ENDIF
	CASE "ローション注入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のアナルに直接ローションを注入します
		ENDIF
	CASE "キス（ドラゴン）"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CSTR:PLAYER:ドラゴン%に%CALLNAME:TARGET%とキスをさせます
		ENDIF
	CASE "フェラさせる（ドラゴン）"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CSTR:PLAYER:ドラゴン%のペニスを%CALLNAME:TARGET%に舐めさせます
		ENDIF
	CASE "舐め回す（ドラゴン）"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CSTR:PLAYER:ドラゴン%の大きな舌で%CALLNAME:TARGET%を舐め回します
		ENDIF
	CASE "睨みつける（ドラゴン）"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CSTR:PLAYER:ドラゴン%の瞳で%CALLNAME:TARGET%を威嚇します
		ENDIF
	CASE "膣を犯す（ドラゴン）"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CSTR:PLAYER:ドラゴン%に%CALLNAME:TARGET%の女性器を犯させます
		ENDIF
	CASE "アナルを犯す（ドラゴン）"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CSTR:PLAYER:ドラゴン%に%CALLNAME:TARGET%のアナルを犯させます
		ENDIF
	CASE "手淫する"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のペニスを手で扱きます
		ENDIF
	CASE "自慰見せつけ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に自慰を見せつけます
		ENDIF
	CASE "秘貝開帳見せつけ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に、自らの膣口を拡げて見せつけます
		ENDIF
	CASE "キスしてもらう"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%にキスをさせます
		ENDIF
	CASE "情欲を煽る"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 言葉や仕草で%CALLNAME:TARGET%の情欲を煽ります
		ENDIF
	CASE "バイブ被挿入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に膣をバイブで責めてもらいます
		ENDIF
	CASE "アナルバイブ被挿入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%にアナルをバイブで責めてもらいます
		ENDIF
	CASE "アナルバイブ被挿入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に電気按摩で責めてもらいます
		ENDIF
	CASE "鞭打ちしてもらう"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に鞭で叩いてもらいます
		ENDIF
	CASE "踏んでもらう"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に踏んでもらいます
		ENDIF
	CASE "叩いてもらう"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に叩いてもらいます
		ENDIF
	CASE "素股する"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のペニスを%CALLNAME:PLAYER%の股間で擦り上げます
		ENDIF
	CASE "パイズリする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のペニスを胸で包んで扱き上げます
		ENDIF
	CASE "パイズリフェラする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のペニスを胸で包んで扱き上げ、同時に口も使って責め立てます
		ENDIF
	CASE "フィストファックさせる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の腕で膣を犯してもらいます
		ENDIF
	CASE "アナルフィストさせる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の腕でアナルを犯してもらいます
		ENDIF
	CASE "触手射精止め"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:射精止め
			ツールチップ = 触手での射精止めを解放します
		ELSE
			ツールチップ = 細い触手で%CALLNAME:TARGET%のペニスを強く締め付け、射精できないようにします
		ENDIF
		ENDIF
	CASE "触手強制搾精"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 細い触手で%CALLNAME:TARGET%の尿道から直接粘液を流し込み、擬似的な射精の快感を与えます
		ENDIF
	CASE "触手強制放尿"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 細い触手で%CALLNAME:TARGET%の尿道から直接液体を流し込み、擬似的に放尿させます
		ENDIF
	CASE "腸洗浄"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の腸内をぬるま湯で洗って綺麗にします
		ENDIF
	CASE "ゲームセンター"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:ゲームセンター
			ツールチップ = %GCS:店名%を後にして自宅に帰ります
		ELSE
			ツールチップ = %CALLNAME:TARGET%と一緒に%GCS:店名%に行きます
		ENDIF
		ENDIF
	CASE "トイレに行く"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:トイレ
			ツールチップ = トイレを出てフロアに戻ります
		ELSE
			ツールチップ = %CALLNAME:TARGET%をトイレに連れ込みます
		ENDIF
		ENDIF
	CASE "IIDXをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にbeatmaniaIIDXをプレイします
		ENDIF
	CASE "ポップンをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にポップンミュージックで遊びます
		ENDIF
	CASE "DDRをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にDance Dance Revolutionをプレイします
		ENDIF
	CASE "ギタドラをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%\@ ASSI > 0 ? 、%CALLNAME:ASSI% #  \@と一緒にギタドラでセッションします
		ENDIF
	CASE "jubeatをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にjubeatで遊びます
		ENDIF
	CASE "リフレクをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にリフレクビートで遊びます
		ENDIF
	CASE "ボルテをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にSOUND VOLTEXで遊びます
		ENDIF
	CASE "ダンエボをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にDance Evolutionで踊ります
		ENDIF
	CASE "魔法の練習"
		IF LANGUAGE == "ENG"
		ELSE
		IF ABL:PLAYER:魔法技能 && ABL:魔法技能
			ツールチップ = %CALLNAME:TARGET%と一緒に魔法の練習をします
		ELSEIF ABL:PLAYER:魔法技能
			ツールチップ = %CALLNAME:TARGET%を相手に魔法の練習をします
		ELSEIF ABL:魔法技能
			ツールチップ = %CALLNAME:TARGET%に魔法の練習をさせます
		ENDIF
		ENDIF
	CASE "魔法詠唱"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 魔力を消費して魔法を詠唱します
		ENDIF
	CASE "料理を作る"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に料理を作らせます
		ENDIF
	CASE "お菓子を作る"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%にお菓子を作らせます
		ENDIF
	CASE "楽器の練習"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に楽器演奏の練習をさせます
		ENDIF
	CASE "機械の勉強"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に電子機械についての指導をします
		ENDIF
	CASE "プログラミング演習"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に、さらに専門的な機械の指導をします
		ENDIF
	CASE "尻尾を撫でる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の尻尾を撫でたりもふもふしたりします
		ENDIF
	CASE "ハッキング"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%をハッキングして特殊な命令を送ります
		ENDIF
	CASE "アナル愛撫させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に%CALLNAME:PLAYER%のアナルを愛撫させます
		ENDIF
	CASE "ペニスバンド"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = ペニスバンドを装着します
		ENDIF
	CASE "電気按摩愛撫させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に電気按摩で%CALLNAME:PLAYER%を愛撫させます
		ENDIF
	CASE "QMAをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にクイズマジックアカデミーで遊びます
		ENDIF
	CASE "maimaiをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にmaimaiで遊びます
		ENDIF
	CASE "CHUNITHMをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にCHUNITHMで遊びます
		ENDIF
	CASE "ビデオポーカーで遊ぶ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = ビデオポーカーで遊びます
		ENDIF
	CASE "ポーカーソリティアで遊ぶ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = ポーカーソリティアで遊びます
		ENDIF
	CASE "モンテカルロで遊ぶ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = モンテカルロで遊びます
		ENDIF
	CASE "ブラックジャックで遊ぶ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = ブラックジャックで遊びます
		ENDIF
	CASE "ミッドナイトスペシャル"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = ミッドナイトスペシャル(精力剤)を使います
		ENDIF
	CASE "排卵マッサージ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 子宮周辺などをマッサージして排卵や着床までの流れがスムーズに行えるようにします
		ENDIF
	CASE "電動エネマグラ"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:電動エネマグラ
			ツールチップ = アナルから電動エネマグラを引き抜きます
		ELSE
			ツールチップ = 前立腺の刺激に特化したエネマグラをアナルに挿入します
		ENDIF
		ENDIF
	CASE "搾精機"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:搾精機
			ツールチップ = ペニスから搾精機を取り外します
		ELSE
			ツールチップ = 精液を搾り取るための搾精機をペニスに装着します
		ENDIF
		ENDIF
	CASE "射精寸止め"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%が射精する寸前で止めます
		ENDIF
	CASE "貞操帯"
		IF LANGUAGE == "ENG"
		ELSE
		IF CFLAG:貞操帯
			ツールチップ = 貞操帯を外します
		ELSE
			ツールチップ = 貞操帯を装着します
		ENDIF
		ENDIF
	CASE "女装させる"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:女装
			ツールチップ = 女装をやめさせ、元の服装に戻します
		ELSE
			ツールチップ = %CALLNAME:TARGET%に女装をさせます
		ENDIF
		ENDIF
	CASE "尿道カテーテル"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:尿道カテーテル
			ツールチップ = 尿道からカテーテルを引き抜きます
		ELSE
			ツールチップ = 尿道にカテーテルを挿入します
		ENDIF
		ENDIF
	CASE "ふたなり化"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = スライムキャラをふたなり化したりふたなり解除します
		ENDIF
	CASE "ローター愛撫させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%にローターで愛撫させます
		ENDIF
	CASE "電気按摩愛撫させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に電気按摩で愛撫させます
		ENDIF
	CASE "ペットに舐めさせる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:ASSI%に%CALLNAME:TARGET%の体を舐めさせます
		ENDIF
	CASE "ペットに犯させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に%CALLNAME:ASSI%をけしかけます
		ENDIF
	CASE "ライブ配信"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:ライブ配信
			ツールチップ = ライブ配信を終了します
		ELSE
			ツールチップ = %CALLNAME:TARGET%の痴態を全世界に生中継します
		ENDIF
		ENDIF
	CASE "尿道挿入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の尿道にペニスを挿入します
		ENDIF
	CASE "二輪挿し"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の膣に%CALLNAME:TMASTER%と%CALLNAME:ASSI%のペニスを同時に挿入します
		ENDIF
	CASE "アナル二輪挿し"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のアナルに%CALLNAME:TMASTER%と%CALLNAME:ASSI%のペニスを同時に挿入します
		ENDIF
	CASE "尿道ポッキー"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の尿道に手作りポッキーを挿入します
		ENDIF
	CASE "裸体盛り"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の裸体に料理を盛り付けます
		ENDIF
	CASE "お掃除フェラする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のペニスを口で綺麗にします
		ENDIF
	CASE "ジュースを飲ませる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%にジュースを飲ませます
		ENDIF
	CASE "採血"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:採血
			ツールチップ = %CALLNAME:TARGET%の採血を中断します
		ELSE
			ツールチップ = %CALLNAME:TARGET%から採血をします
		ENDIF
		ENDIF
	CASE "アナル双頭ディルド"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%のアナルと%CALLNAME:PLAYER%のアナルに双頭ディルドを挿入します
		ENDIF
	CASE "双頭ディルド"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の膣と%CALLNAME:PLAYER%の膣に双頭ディルドを挿入します
		ENDIF
	CASE "名刺を渡す"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に名刺を渡して常連客候補にします
		ENDIF
	CASE "メダルを借りる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = お金をメダルに替えます
		ENDIF
	CASE "シックスナイン"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と互いの性器を舐め合います
		ENDIF
	CASE "69パイズリ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と互いの性器を舐め合いながらパイズリもさせます
		ENDIF
	CASE "岩清水"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に顔面騎乗させ、愛液を啜ります
		ENDIF
	CASE "Gスポット刺激"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の膣内の敏感な部分を指で刺激します
		ENDIF
	CASE "搾乳"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の母乳を絞ります
		ENDIF
	CASE "授乳手コキ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の\@ TALENT:TARGET:母乳体質 ? 母乳を飲みながら # 乳首を吸いながら \@手淫してもらいます
		ENDIF
	CASE "前立腺刺激"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の前立腺を指で刺激します
		ENDIF
	CASE "オナフェラ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%にフェラチオさせながら自慰もさせます
		ENDIF
	CASE "乱れ牡丹"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 鏡の前で激しく%CALLNAME:TARGET%と背面座位をします
		ENDIF
	CASE "騎乗乳房自慰"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に騎乗位させながら胸での自慰をさせます
		ENDIF
	CASE "挿入Gスポ責め"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の膣内に挿入しながらさらに敏感な部分を責め立てます
		ENDIF
	CASE "挿入子宮口責め"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の膣内の最奥を執拗に突きます
		ENDIF
	CASE "正常位キス", "正常位アナルキス"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に正常位しながらキスします
		ENDIF
	CASE "騎乗位キス", "騎乗位アナルキス"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に騎乗位させながらキスします
		ENDIF
	CASE "対面座位キス", "対面座位アナルキス"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と対面座位しながらキスします
		ENDIF
	CASE "後背位手淫", "後背位アナル手淫"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%を後ろから犯しながら手淫します
		ENDIF
	CASE "騎乗位手淫", "騎乗位アナル手淫"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に騎乗位させながら手淫します
		ENDIF
	CASE "正常位キスさせる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に正常位させながらキスしてもらいます
		ENDIF
	CASE "逆レイプキス"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に逆レイプしながらキスします
		ENDIF
	CASE "対面座位吸血"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と対面座位しながら吸血します
		ENDIF
	CASE "背面座位吸血"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に背面座位しながら吸血します
		ENDIF
	CASE "正常位吸血させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に正常位させながら吸血させます
		ENDIF
	CASE "後背位吸血させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に後背位させながら吸血させます
		ENDIF
	CASE "3P"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:ASSI%を含めた3人で乱交します
		ENDIF
	CASE "ダブル素股"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と%CALLNAME:(TFLAG:前コマンド実行者)%の陰唇でペニスを扱いてもらいます
		ENDIF
	CASE "二本フェラ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に%CALLNAME:TMASTER%と%CALLNAME:ASSI%のペニスを同時に舐めさせます
		ENDIF
	CASE "数珠繋ぎ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 3人で繋がるように挿入します
		ENDIF
	CASE "正常位首絞め", "後背位首絞め", "正常位アナル首絞め", "後背位アナル首絞め", "逆レイプ首絞め"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 性交しながら相手の首を絞めます
		ENDIF
	CASE "首絞め愛撫"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の首を絞めながら愛撫します
		ENDIF
	CASE "蜂蜜ローション"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の体に蜂蜜を塗ります
		ENDIF
	CASE "バストサイズ変更"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = [溶ける快]を持つキャラのバストサイズを変更します
		ENDIF
	CASE "乳首舐め"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の乳首を舐めます
		ENDIF
	CASE "スライム尿道責め"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の尿道をスライムの体液で塞ぎます
		ENDIF
	CASE "コナステで遊ぶ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にコナステの音ゲーを遊びます
		ENDIF
	CASE "蘇生薬投与"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%を仮死状態から蘇生します
		ENDIF
	CASE "オンゲキをプレイする"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%と一緒にオンゲキをプレイします
		ENDIF
	CASE "臍愛撫"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の臍を愛撫します
		ENDIF
	CASE "臍挿入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の臍にペニス\@ !PENIS(PLAYER) ? バンド #\@を挿入します
		ENDIF
	CASE "乳管挿入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の乳管にペニス\@ !PENIS(PLAYER) ? バンド #\@を挿入します
		ENDIF
	CASE "臍針挿入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の臍に針を挿入します
		ENDIF
	CASE "乳管針挿入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の乳管に針を挿入します
		ENDIF
	CASE "臍フィスト"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の臍に拳を挿入します
		ENDIF
	CASE "尿道針挿入"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の尿道に針を挿入します
		ENDIF
	CASE "触手臍挿入"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:触手臍挿入
			ツールチップ = %CALLNAME:TARGET%の臍から触手を引き抜きます
		ELSE
			ツールチップ = %CALLNAME:TARGET%の臍に触手を挿入します
		ENDIF
		ENDIF
	CASE "触手乳管挿入"
		IF LANGUAGE == "ENG"
		ELSE
		IF TEQUIP:触手乳管挿入
			ツールチップ = %CALLNAME:TARGET%の乳管から触手を引き抜きます
		ELSE
			ツールチップ = %CALLNAME:TARGET%の乳管に触手を挿入します
		ENDIF
		ENDIF
	CASE "恐怖を煽る"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 道具などを使って%CALLNAME:TARGET%の恐怖を煽ります
		ENDIF
	CASE "根性焼き"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に煙草の火を押し付けます
		ENDIF
	CASE "飲尿する"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%の尿を飲みます
		ENDIF
	CASE "飲尿させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%に尿を飲ませます
		ENDIF
	CASE "体内放尿する"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%にペニスを挿入したまま放尿します
		ENDIF
	CASE "体内放尿させる"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = %CALLNAME:TARGET%にペニスを挿入されたまま放尿してもらいます
		ENDIF
	CASE "テンプレ"
		IF LANGUAGE == "ENG"
		ELSE
		ツールチップ = 
		ENDIF
	CASEELSE
		IF LANGUAGE == "ENG"
		ELSE
		;#; DEBUGPRINTFORML %コマンド名%にツールチップが設定されていません
		ENDIF
		ツールチップ = 
ENDSELECT

RETURNF ツールチップ
