;-------------------------------------------------
;触手召喚
;-------------------------------------------------
@MESSAGE_COM_触手召喚
;解除時
IF TEQUIP:触手
	PRINTFORMW 触手を送り還し、%CALLNAME:TARGET%の\@ CFLAG:死亡 ? 死体 # 体 \@を解放した……
ELSE
	PRINTFORMW %CALLNAME:PLAYER%の呼び出した異形の触手が、%CALLNAME:TARGET%の\@ CFLAG:死亡 ? 死体 # 体 \@を絡め取った……
ENDIF

;-------------------------------------------------
;触手挿入
;-------------------------------------------------
@MESSAGE_COM_触手挿入
;解除時
IF TEQUIP:バイブ
	PRINTFORMW 触手が%CALLNAME:TARGET%の女性器から引き抜かれた
ELSE
	PRINT 細かい突起に覆われた触手が
	SIF !性器露出(TARGET)
		PRINT 衣服の間から
	PRINTFORMW %CALLNAME:TARGET%の女性器に潜り込んでいった
ENDIF

;-------------------------------------------------
;アナル触手
;-------------------------------------------------
@MESSAGE_COM_アナル触手
;解除時
IF TEQUIP:アナルバイブ
	PRINTFORMW 触手は%CALLNAME:TARGET%のアヌスから引き抜かれた
ELSE
	PRINT 長大な触手が
	SIF !性器露出(TARGET)
		PRINT 衣服の間から
	PRINTFORMW %CALLNAME:TARGET%のアナルに潜り込んだ
ENDIF

;-------------------------------------------------
;触手クリ責め
;-------------------------------------------------
@MESSAGE_COM_触手クリ責め
;解除時
IF TEQUIP:クリキャップ
	PRINTFORMW 触手は%CALLNAME:TARGET%のクリ責めを中断した
ELSE
	PRINT 細かく枝分かれした触手が
	SIF !性器露出(TARGET)
		PRINT 衣服の間から入り込み、
	PRINTFORMW %CALLNAME:TARGET%のクリトリスを責めはじめた
ENDIF

;-------------------------------------------------
;触手乳首責め
;-------------------------------------------------
@MESSAGE_COM_触手乳首責め
;解除時
IF TEQUIP:ニプルキャップ
	PRINTFORMW 触手は%CALLNAME:TARGET%の乳首から離れた
ELSE
	PRINTFORMW 細かく蠢く触手が%CALLNAME:TARGET%の乳首を弄り始めた
ENDIF

;-------------------------------------------------
;触手搾乳
;-------------------------------------------------
@MESSAGE_COM_触手搾乳
;解除時
IF TEQUIP:搾乳器
	PRINTFORMW 触手は%CALLNAME:TARGET%の乳首を解放した
ELSE
	PRINT 吸盤状の触手が
	SIF !性器露出(TARGET)
		PRINT 衣服の間から入り込み、
	PRINTFORMW %CALLNAME:TARGET%の乳首を吸いあげはじめた
ENDIF

;-------------------------------------------------
;触手緊縛
;-------------------------------------------------
@MESSAGE_COM_触手緊縛
;解除時
IF TEQUIP:縄
	PRINTFORMW 触手は%CALLNAME:TARGET%の拘束を解いた
ELSE
	PRINT 無数の触手が
	SIF !性器露出(TARGET)
		PRINT 衣服の上から
	PRINTFORMW %CALLNAME:TARGET%の体を縛りあげた
ENDIF

;-------------------------------------------------
;触手浣腸
;-------------------------------------------------
@MESSAGE_COM_触手浣腸
;解除時
IF TEQUIP:浣腸器
	PRINTFORML %CALLNAME:TARGET%は触手を抜かれ、粘液に塗れた腸内の汚物を肛門から撒き散らした
	;マゾっ気
	SELECTCASE ABL:マゾっ気
		CASE 0
			PRINTFORMW %CALLNAME:TARGET%は苦悶と恥辱の表情を浮かべている
		CASE 1
			PRINTFORMW %CALLNAME:TARGET%は脂汗を浮かべ、いまだ下腹に残る排泄感に悶えている
		CASE 2
			PRINTFORMW %CALLNAME:TARGET%は恥ずかしがりながらも、開放感にほっとした表情を浮かべている
		CASE 3
			PRINTFORMW %CALLNAME:TARGET%は肛門を通り過ぎる排泄物の感覚に時折艶っぽい喘ぎ声を上げている
		CASE 4
			PRINTFORMW %CALLNAME:TARGET%はうっとりした表情を浮かべ、排泄感を愉しんでいる
		CASE IS >= 5
			PRINTFORML %CALLNAME:TARGET%は排泄感と恥辱を心ゆくまで味わっている
			PRINTFORMW %CALLNAME:TARGET%の表情は快感にだらしなく緩み、肛門から汚物をひりだすと同時に、体中の穴から感極まる汁を垂れ流している
	ENDSELECT
ELSE
	PRINTFORMW 極太の触手が%CALLNAME:TARGET%の菊穴をふさぎ、体液を流し込んだ
ENDIF

;-------------------------------------------------
;触手口辱
;-------------------------------------------------
@MESSAGE_COM_触手口辱
;解除時
IF TEQUIP:触手口辱
	PRINTFORMW 触手は%CALLNAME:TARGET%の口から引き抜かれた
ELSE
	PRINTFORMW 濡れ光る触手が%CALLNAME:TARGET%の口に侵入した
ENDIF

;-------------------------------------------------
;触手搾精
;-------------------------------------------------
@MESSAGE_COM_触手搾精
;解除時
IF TEQUIP:オナホール
	PRINTFORMW 触手は%CALLNAME:TARGET%のペニスを解放した
ELSE
	SELECTCASE RAND:2
		CASE 0
			PRINTFORM いやらしく肉ヒダをうねらせた触手が%CALLNAME:TARGET%の
			SIF !性器露出(TARGET)
				PRINT 衣服の中に入り込み
			PRINTW ペニスをぱっくりと咥え込んだ
		CASE 1
			PRINTFORM ぬめる触手が%CALLNAME:TARGET%の
			SIF !性器露出(TARGET)
				PRINT 衣服の中に入り込むと
			PRINTW ペニスを咥え込み、精液を搾り取るかのように吸い付いている……
	ENDSELECT
ENDIF

;-------------------------------------------------
;触手射精止め
;-------------------------------------------------
@MESSAGE_COM_触手射精止め
IF TEQUIP:射精止め
	PRINTFORMW %CALLNAME:TARGET%の射精を封じる触手を解放した
ELSE
	PRINTFORMW 細めの触手が%CALLNAME:TARGET%のペニス\@ SEX(TARGET) == 1 ? と陰嚢 #  \@に絡みつき、射精が出来ないように強く締め上げた……
ENDIF

;-------------------------------------------------
;触手強制搾精
;-------------------------------------------------
@MESSAGE_COM_触手強制搾精
PRINTFORMW 細い触手が%CALLNAME:TARGET%の鈴口に差し込まれると、多量の粘液を流し込んで精嚢を犯した……

;-------------------------------------------------
;触手強制放尿
;-------------------------------------------------
@MESSAGE_COM_触手強制放尿
PRINTFORMW 細い触手が%CALLNAME:TARGET%の尿道に差し込まれると、多量の体液を流し込んで膀胱を犯した……

;-------------------------------------------------
;触手臍挿入
;-------------------------------------------------
@MESSAGE_COM_触手臍挿入
IF TEQUIP:触手臍挿入
	PRINTFORMW 触手が%CALLNAME:TARGET%の臍から引き抜かれると、粘液混じりの汚液が噴き出し、下腹部を伝った……
ELSE
	PRINTFORMW 触手が%CALLNAME:TARGET%の臍の窪みに潜り込み、内臓を蹂躙し始めた……
ENDIF

;-------------------------------------------------
;触手乳管挿入
;-------------------------------------------------
@MESSAGE_COM_触手乳管挿入
IF TEQUIP:触手乳管挿入
	PRINTFORM 触手が%CALLNAME:TARGET%の乳管から引き抜かれると、
	IF TALENT:母乳体質
		PRINT 粘液混じりの白濁した母乳が
	ELSE
		PRINT 乳首から粘液が
	ENDIF
	PRINTW 噴き出し、乳房を伝っている……
ELSE
	PRINTFORMW 触手が%CALLNAME:TARGET%の乳房に絡みつき、乳管から潜り込んだ……
ENDIF
