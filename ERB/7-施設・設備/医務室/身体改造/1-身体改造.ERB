@BODY_MOD

JUMP BODY_MOD_1

DRAWLINE

PRINTL □施す改造の分野を選んでください
PRINTL 　[1] - 生殖関連（一部未完成です）
PRINTL 　[2] - 体質関連（未完成です）
DRAWLINE
PRINTPLAIN 　BODY MOD　
PRINTL [0] - 戻る

BINPUT
SELECTCASE RESULT
	CASE 0
		RETURN 0
	CASE 1 TO 2
		CALLFORM BODY_MOD_{RESULT}
		RESTART
ENDSELECT

@身体改造口上, 改造項目
#DIM DYNAMIC SKIP
#DIM DYNAMIC LCOUNT
#DIMS DYNAMIC 改造項目

IF CMATCH(CSTR:配属, "医務室")
	SKIP = RAND:CMATCH(CSTR:配属, "医務室")
	FOR LCOUNT, 0, CHARANUM
		IF CSTR:LCOUNT:配属 == "医務室"
			SIF SKIP--
				CONTINUE
			CALL KOJO_STRATEGY, LCOUNT, "医務室", @"身体改造_%改造項目%"
			BREAK
		ENDIF
	NEXT
ENDIF
