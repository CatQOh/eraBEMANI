@BODY_MOD

JUMP BODY_MOD_1

DRAWLINE

PRINTL □施す改造の分野を選んでください
PRINTL 　[1] - 生殖関連（一部未完成です）
PRINTL 　[2] - 体質関連（未完成です）
DRAWLINE
PRINTPLAIN 　BODY MOD　
PRINTL [0] - 戻る

BINPUT
SELECTCASE RESULT
	CASE 0
		RETURN 0
	CASE 1 TO 2
		CALLFORM BODY_MOD_{RESULT}
		RESTART
ENDSELECT

@身体改造口上, ARGS
#DIM DYNAMIC LTARGET
#DIM DYNAMIC SKIP
#DIM DYNAMIC LCOUNT

IF CMATCH(CSTR:配属, "医務室")
	SKIP = RAND:CMATCH(CSTR:配属, "医務室")
	FOR LCOUNT, 0, CHARANUM
		IF CSTR:LCOUNT:配属 == "医務室"
			SIF SKIP--
				CONTINUE
			LTARGET = TARGET
			TARGET = LCOUNT
			TRYCALLFORM 医務室_身体改造_%ARGS%_%CSVNAMERPS(NO:LCOUNT)%
			TARGET = LTARGET
			BREAK
		ENDIF
	NEXT
ENDIF
