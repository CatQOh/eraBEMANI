;識汁新婚モード判定
@ZILCH_SHIKI
#FUNCTION
IF ゲームモード() == "新婚モード" && GROUPMATCH("ジルチ", CHARANAMEF(MASTER), CHARANAMEF(TARGET)) && GROUPMATCH("識", CHARANAMEF(MASTER), CHARANAMEF(TARGET))
  RETURNF 1
ELSE
  RETURNF 0
ENDIF

;軽汁新婚モード判定
@ZILCH_KENA
#FUNCTION
IF ゲームモード() == "新婚モード" && GROUPMATCH("ジルチ", CHARANAMEF(MASTER), CHARANAMEF(TARGET)) && GROUPMATCH("光伊 計名", CHARANAMEF(MASTER), CHARANAMEF(TARGET))
  RETURNF 1
ELSE
  RETURNF 0
ENDIF

;経営モード専用。衣裳がビストローバーでのシェフ服固定になる
;IF ゲームモード() == "経営モード"
;	IF パートナー > 0 && FEVE:0
;		CSTR:特殊 = スカーフ
;		CSTR:上半身上着 = シェフコート
;		CSTR:下半身上着 = スラックス
;		CSTR:全身上着 = エプロン
;		CSTR:上半身下着 = タンクトップ
;		CSTR:下半身下着 = トランクス
;		CSTR:足元 = コックシューズ
;		CFLAG:挿入可 = 0
;		CFLAG:着衣併用 = 0
;		ZILCH:18 = 0
;	ENDIF
;ENDIF