@ZILCH_SHIKI, ARG
#FUNCTION
IF ゲームモード() == "新婚モード" && GROUPMATCH("ジルチ", CHARANAMEF(MASTER), CHARANAMEF(TARGET)) && GROUPMATCH("識", CHARANAMEF(MASTER), CHARANAMEF(TARGET))
  RETURNF 1
ELSE
  RETURNF 0
ENDIF