;-------------------------------------------------
;ヨシダさん口上（キャラクターの名前を入れる、下にコンセプトなど一言あれば入れる）
;すみませんただの一発ネタです。まさかの口上アリ！？と期待された方には申し訳ない。
;--------------------------------------------------
;口上の存在判定 X1だけ置換して、処理は書き加えないでください
;--------------------------------------------------
;@EXIST_KOJO_ヨシダさん

;--------------------------------------------------
;調教開始時のセリフ FEVE:0を使用
;--------------------------------------------------
@KOJO_START_ヨシダさん
SIF OPTION:口上表示 == 0
	RETURN 0

;口上で強制的に地の文の表示設定を変更したい場合はここ
;OPTION:地の文表示 = 0 で地の文を表示しない
;OPTION:地の文表示 = 1 で地の文を表示する
;OPTION:地の文表示 = 

;--------------------------------------------------
;エキストラモード構文　FEVE:15を使用
;はいを選択した時に初調教時に口上設定用の素質に変更できます
;下記に処女化する場合の構文をサンプルとして載せておきます
;--------------------------------------------------
;IF FEVE:15 == 0
;	PRINTL 処女モードに移行しますか？
;	PRINTL [0] はい
;	PRINTL [1] いいえ
;	$INPUT_LOOP
;	INPUT
;	IF RESULT == 0
;		PRINTFORMW ―処女モードに素質、能力を変更します―
;		TALENT:TARGET:処女 = 1
;		ABL:TARGET:Ｖ感覚 = 0
;		EXP:TARGET:Ｖ経験 = 0
;	ELSEIF RESULT == 1
;		PRINTFORMW ―通常モードで調教を開始します―
;	ELSE
;		GOTO INPUT_LOOP
;	ENDIF
;	FEVE:15 = 1
;ENDIF
;-------------------------------------------------
;調教開始時 FEVE:0
;-------------------------------------------------
;初調教時
IF FEVE:0 == 0
	PRINTW 同業者と話している時に、その場にいない奴隷の話題。
	PRINTW 登録された奴隷候補の数は既に600を超えており、同業者たちも把握しきれないわけでして
	PRINTW 話題にしようとしているその奴隷の名前が思い浮かばない時、
	PRINTW 「あー・・・んー・・・そうそう・・・名前・・・なんだっけなー」
	PRINTW という事がある時、ある同業者は常に「ヨシダさん」にする癖があります。
	PRINTW …これから綴られる調教の記録。
	PRINTW これは、そんな同業者に、そしてこんな自分に、勝手に「ヨシダさん」にされてしまった人へのちん鎮魂歌です。
	FEVE:0 = 1
ENDIF

RETURN 0
