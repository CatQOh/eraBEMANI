;識×ケイナ新婚モード判定
@KENA_SHIKI
#FUNCTION
IF ゲームモード() == "新婚モード" && GROUPMATCH("光伊 計名", CHARANAMEF(MASTER), CHARANAMEF(TARGET)) && GROUPMATCH("識", CHARANAMEF(MASTER), CHARANAMEF(TARGET))
  RETURNF 1
ELSE
  RETURNF 0
ENDIF

;ジルチ×ケイナ新婚モード判定
@KENA_ZILCH
#FUNCTION
IF ゲームモード() == "新婚モード" && GROUPMATCH("光伊 計名", CHARANAMEF(MASTER), CHARANAMEF(TARGET)) && GROUPMATCH("ジルチ", CHARANAMEF(MASTER), CHARANAMEF(TARGET))
  RETURNF 1
ELSE
  RETURNF 0
ENDIF
