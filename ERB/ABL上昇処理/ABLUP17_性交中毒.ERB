@ABLUP17
#DIM DYNAMIC LCOUNT
SIF ABL:性交中毒 == 10 && !TALENT:リミットバースト
	RETURN 0
SIF ABL:性交中毒 < 0
	RETURN 0
CALL NEED_JUEL_性交中毒
CALL CHECK_JUEL_性交中毒
IF RESULT
	CALL NEED_JUEL_性交中毒
	FOR LCOUNT, 0, 15
		JUEL:LCOUNT -= ABLUP:LCOUNT
	NEXT
	ABL:性交中毒++
ENDIF

@NEED_JUEL_性交中毒
SELECTCASE ABL:性交中毒
	CASE 0
		ABLUP:GETPALAM("恭順") = 10000
		ABLUP:GETPALAM("欲情") = 25000
		ABLUP:GETPALAM("屈服") = 10000
		ABLUPEXP:GETEXP("性交経験") = 10
	CASE 1
		ABLUP:GETPALAM("恭順") = 25000
		ABLUP:GETPALAM("欲情") = 50000
		ABLUP:GETPALAM("屈服") = 25000
		ABLUPEXP:GETEXP("性交経験") = 25
	CASE 2
		ABLUP:GETPALAM("恭順") = 48000
		ABLUP:GETPALAM("欲情") = 90000
		ABLUP:GETPALAM("屈服") = 48000
		ABLUPEXP:GETEXP("性交経験") = 80
	CASE 3
		ABLUP:GETPALAM("恭順") = 100000
		ABLUP:GETPALAM("欲情") = 170000
		ABLUP:GETPALAM("屈服") = 100000
		ABLUPEXP:GETEXP("性交経験") = 150
	CASE 4
		ABLUP:GETPALAM("恭順") = 250000
		ABLUP:GETPALAM("欲情") = 300000
		ABLUP:GETPALAM("屈服") = 250000
		ABLUPEXP:GETEXP("性交経験") = 250
	CASE 5
		ABLUP:GETPALAM("恭順") = 300000
		ABLUP:GETPALAM("欲情") = 350000
		ABLUP:GETPALAM("屈服") = 300000
		ABLUPEXP:GETEXP("性交経験") = 350
	CASE 6
		ABLUP:GETPALAM("恭順") = 350000
		ABLUP:GETPALAM("欲情") = 400000
		ABLUP:GETPALAM("屈服") = 350000
		ABLUPEXP:GETEXP("性交経験") = 500
	CASE 7
		ABLUP:GETPALAM("恭順") = 400000
		ABLUP:GETPALAM("欲情") = 450000
		ABLUP:GETPALAM("屈服") = 400000
		ABLUPEXP:GETEXP("性交経験") = 600
	CASE 8
		ABLUP:GETPALAM("恭順") = 450000
		ABLUP:GETPALAM("欲情") = 500000
		ABLUP:GETPALAM("屈服") = 450000
		ABLUPEXP:GETEXP("性交経験") = 700
	CASE 9
		ABLUP:GETPALAM("恭順") = 500000
		ABLUP:GETPALAM("欲情") = 600000
		ABLUP:GETPALAM("屈服") = 500000
		ABLUPEXP:GETEXP("性交経験") = 800
	CASE IS >= 10
		LOCAL = ABL:性交中毒*ABL:性交中毒
		ABLUP:GETPALAM("恭順") = 6000 * LOCAL
		ABLUP:GETPALAM("欲情") = 7000 * LOCAL
		ABLUP:GETPALAM("屈服") = 6000 * LOCAL
		ABLUPEXP:GETEXP("性交経験") = 9 * LOCAL
ENDSELECT

;一線越えない
IF TALENT:一線越えない
	;未陥落
	IF !CFLAG:陥落
		TIMES ABLUP:GETPALAM("恭順") , 2.00
		TIMES ABLUP:GETPALAM("欲情") , 2.00
		TIMES ABLUP:GETPALAM("屈服") , 2.00
		TIMES ABLUPEXP:GETEXP("性交経験") , 2.00
	;陥落
	ELSEIF CFLAG:陥落
		TIMES ABLUP:GETPALAM("恭順") , 0.80
		TIMES ABLUP:GETPALAM("欲情") , 0.80
		TIMES ABLUP:GETPALAM("屈服") , 0.80
		TIMES ABLUPEXP:GETEXP("性交経験") , 0.80
	ENDIF
ENDIF
;自制心
IF TALENT:自制心
	;欲望LV4以下
	IF ABL:欲望 <= 4
		TIMES ABLUP:GETPALAM("恭順") , 2.00
		TIMES ABLUP:GETPALAM("欲情") , 2.00
		TIMES ABLUP:GETPALAM("屈服") , 2.00
		TIMES ABLUPEXP:GETEXP("性交経験") , 2.00
	;欲望LV5以上
	ELSEIF ABL:欲望 >= 5
		TIMES ABLUP:GETPALAM("恭順") , 0.80
		TIMES ABLUP:GETPALAM("欲情") , 0.80
		TIMES ABLUP:GETPALAM("屈服") , 0.80
		TIMES ABLUPEXP:GETEXP("性交経験") , 0.80
	ENDIF
ENDIF
;中毒しやすい
IF TALENT:中毒しやすい
	TIMES ABLUP:GETPALAM("恭順") , 0.50
	TIMES ABLUP:GETPALAM("欲情") , 0.50
	TIMES ABLUP:GETPALAM("屈服") , 0.50
	TIMES ABLUPEXP:GETEXP("性交経験") , 0.50
ENDIF
;男
IF SEX(TARGET) == 1
	TIMES ABLUP:GETPALAM("恭順") , 0.80
	TIMES ABLUP:GETPALAM("欲情") , 0.80
	TIMES ABLUP:GETPALAM("屈服") , 0.80
	TIMES ABLUPEXP:GETEXP("性交経験") , 1.50
ENDIF
;淫膣、淫尻
IF TALENT:淫膣 || TALENT:淫尻
	TIMES ABLUP:GETPALAM("恭順") , 0.70
	TIMES ABLUP:GETPALAM("欲情") , 0.70
	TIMES ABLUP:GETPALAM("屈服") , 0.70
	TIMES ABLUPEXP:GETEXP("性交経験") , 0.70
ENDIF

;LV2からは異常経験必要 LV3からは異常経験2以上必要 LV4からは異常経験3以上必要 LV8からは異常経験7以上必要（素質：[中毒しやすい][淫膣][淫尻]なら無視できる）
IF !TALENT:中毒しやすい && !TALENT:淫膣 && !TALENT:淫尻
	SELECTCASE ABL:性交中毒
		CASE IS >= 8
			ABLUPEXP:GETEXP("異常経験") = 7
		CASE IS >= 4
			ABLUPEXP:GETEXP("異常経験") = 3
		CASE IS >= 3
			ABLUPEXP:GETEXP("異常経験") = 2
		CASE IS >= 2
			ABLUPEXP:GETEXP("異常経験") = 1
	ENDSELECT
ENDIF

@CHECK_JUEL_性交中毒
;異常経験が必要
SIF EXP:異常経験 < ABLUPEXP:GETEXP("異常経験")
	RETURN 0

;恭順、欲情、屈服の珠及び性交経験で上げる
SIF JUEL:恭順 >= ABLUP:GETPALAM("恭順") && JUEL:欲情 >= ABLUP:GETPALAM("欲情") && JUEL:屈服 >= ABLUP:GETPALAM("屈服") && EXP:性交経験 >= ABLUPEXP:GETEXP("性交経験")
	RETURN 1

;上記の条件を満たせないと上げられない
RETURN 0

@ABLUP_MESSAGE_性交中毒
SELECTCASE ABL:性交中毒
	CASE 2, 3, 4, 8
		;LV2からは異常経験必要 LV3からは異常経験2以上必要 LV4からは異常経験3以上必要 LV8からは異常経験7以上必要（素質：[中毒しやすい][淫膣]なら無視できる）
		SIF EXP:異常経験 < ABLUPEXP:GETEXP("異常経験")
			SETCOLORBYNAME gray
		SIF ABLUPEXP:GETEXP("異常経験")
			PRINTFORM [異常経験{ABLUPEXP:GETEXP("異常経験")}以上]
		RESETCOLOR
ENDSELECT

SIF JUEL:恭順 < ABLUP:GETPALAM("恭順")
	SETCOLORBYNAME gray
PRINTFORM [恭順の珠×{ABLUP:GETPALAM("恭順")}]
RESETCOLOR

SIF JUEL:欲情 < ABLUP:GETPALAM("欲情")
	SETCOLORBYNAME gray
PRINTFORM [欲情の珠×{ABLUP:GETPALAM("欲情")}]
RESETCOLOR

SIF JUEL:屈服 < ABLUP:GETPALAM("屈服")
	SETCOLORBYNAME gray
PRINTFORM [屈服の珠×{ABLUP:GETPALAM("屈服")}]
RESETCOLOR

SIF EXP:性交経験 < ABLUPEXP:GETEXP("性交経験")
	SETCOLORBYNAME gray
PRINTFORM [性交経験{ABLUPEXP:GETEXP("性交経験")}以上]
RESETCOLOR

