@ABLUP19
SIF ABL:メス度 == 10 && !TALENT:リミットバースト
	RETURN 0
CALL NEED_JUEL_メス度
CALL CHECK_JUEL_メス度
IF RESULT
	CALL NEED_JUEL_メス度
	REPEAT 15
		JUEL:COUNT -= ABLUP:COUNT
	REND
	ABL:メス度++
ENDIF

@NEED_JUEL_メス度
;噴乳中毒のコピーしただけなので保留
SELECTCASE ABL:メス度
	CASE 0
		ABLUP:GETPALAM("快Ａ") = 5000
		ABLUP:GETPALAM("屈服") = 3000
		ABLUPEXP:GETEXP("Ａ経験") = 50
		ABLUPEXP:GETEXP("射精管理経験") = 30
	CASE 1
		ABLUP:GETPALAM("快Ａ") = 20000
		ABLUP:GETPALAM("屈服") = 10000
		ABLUPEXP:GETEXP("Ａ経験") = 100
		ABLUPEXP:GETEXP("射精管理経験") = 50
	CASE 2
		ABLUP:GETPALAM("快Ａ") = 50000
		ABLUP:GETPALAM("屈服") = 20000
		ABLUPEXP:GETEXP("Ａ経験") = 150
		ABLUPEXP:GETEXP("射精管理経験") = 100
	CASE 3
		ABLUP:GETPALAM("快Ａ") = 100000
		ABLUP:GETPALAM("屈服") = 30000
		ABLUPEXP:GETEXP("Ａ経験") = 250
		ABLUPEXP:GETEXP("射精管理経験") = 200
	CASE 4
		ABLUP:GETPALAM("快Ａ") = 150000
		ABLUP:GETPALAM("屈服") = 50000
		ABLUPEXP:GETEXP("Ａ経験") = 350
		ABLUPEXP:GETEXP("射精管理経験") = 300
	CASE 5
		ABLUP:GETPALAM("快Ａ") = 200000
		ABLUP:GETPALAM("屈服") = 70000
		ABLUPEXP:GETEXP("Ａ経験") = 500
		ABLUPEXP:GETEXP("射精管理経験") = 500
	CASE 6
		ABLUP:GETPALAM("快Ａ") = 300000
		ABLUP:GETPALAM("屈服") = 100000
		ABLUPEXP:GETEXP("Ａ経験") = 600
		ABLUPEXP:GETEXP("射精管理経験") = 700
	CASE 7
		ABLUP:GETPALAM("快Ａ") = 500000
		ABLUP:GETPALAM("屈服") = 150000
		ABLUPEXP:GETEXP("Ａ経験") = 750
		ABLUPEXP:GETEXP("射精管理経験") = 900
	CASE 8
		ABLUP:GETPALAM("快Ａ") = 700000
		ABLUP:GETPALAM("屈服") = 200000
		ABLUPEXP:GETEXP("Ａ経験") = 850
		ABLUPEXP:GETEXP("射精管理経験") = 1200
	CASE 9
		ABLUP:GETPALAM("快Ａ") = 1000000
		ABLUP:GETPALAM("屈服") = 300000
		ABLUPEXP:GETEXP("Ａ経験") = 1000
		ABLUPEXP:GETEXP("射精管理経験") = 1500
	CASE IS >= 10
		LOCAL = ABL:メス度*ABL:メス度
		ABLUP:GETPALAM("快Ａ") = LOCAL*12000
		ABLUP:GETPALAM("屈服") = LOCAL*5000
		ABLUPEXP:GETEXP("Ａ経験") = LOCAL*15
		ABLUPEXP:GETEXP("射精管理経験") = LOCAL*20
ENDSELECT

;性転換済み
IF TALENT:性転換済
	TIMES ABLUP:GETPALAM("快Ａ") , 0.70
	TIMES ABLUP:GETPALAM("屈服") , 0.70
	TIMES ABLUPEXP:GETEXP("Ａ経験") , 0.70
	TIMES ABLUPEXP:GETEXP("射精管理経験") , 0.70
ENDIF
;去勢済み
IF TALENT:去勢済み
	TIMES ABLUP:GETPALAM("快Ａ") , 0.50
	TIMES ABLUP:GETPALAM("屈服") , 0.50
	TIMES ABLUPEXP:GETEXP("Ａ経験") , 0.50
	ABLUPEXP:GETEXP("射精管理経験") = 0
ENDIF

@CHECK_JUEL_メス度
;快Ａ、屈服の珠、Ａ経験、射精管理経験で上げる
SIF JUEL:快Ａ >= ABLUP:GETPALAM("快Ａ") && JUEL:屈服 >= ABLUP:GETPALAM("屈服") && EXP:Ａ経験 >= ABLUPEXP:GETEXP("Ａ経験") && EXP:射精管理経験 >= ABLUPEXP:GETEXP("射精管理経験")
	RETURN 1

;上記の条件を満たさないと上げられない
RETURN 0

@ABLUP_MESSAGE_メス度
SIF JUEL:快Ａ < ABLUP:GETPALAM("快Ａ")
	SETCOLORBYNAME gray
PRINTFORM [快Ａの珠×{ABLUP:GETPALAM("快Ａ")}]
RESETCOLOR

SIF JUEL:屈服 < ABLUP:GETPALAM("屈服")
	SETCOLORBYNAME gray
PRINTFORM [屈服の珠×{ABLUP:GETPALAM("屈服")}]
RESETCOLOR

SIF EXP:Ａ経験 < ABLUPEXP:GETEXP("Ａ経験")
	SETCOLORBYNAME gray
PRINTFORM [Ａ経験{ABLUPEXP:GETEXP("Ａ経験")}以上]
RESETCOLOR

SIF EXP:射精管理経験 < ABLUPEXP:GETEXP("射精管理経験")
	SETCOLORBYNAME gray
PRINTFORM [射精管理経験{ABLUPEXP:GETEXP("射精管理経験")}以上]
RESETCOLOR
