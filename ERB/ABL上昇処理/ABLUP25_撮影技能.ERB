@ABLUP25
SIF ABL:撮影技能 == 10 && !TALENT:リミットバースト
	RETURN 0
CALL NEED_JUEL25
CALL CHECK_JUEL25
IF RESULT
	CALL NEED_JUEL25
	REPEAT 15
		JUEL:COUNT -= ABLUP:COUNT
	REND
	ABL:撮影技能++
ENDIF

@NEED_JUEL25
SELECTCASE ABL:撮影技能
	CASE 0
		ABLUP:GETNUM(PALAM, "習得") = 75
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 5
	CASE 1
		ABLUP:GETNUM(PALAM, "習得") = 500
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 10
	CASE 2
		ABLUP:GETNUM(PALAM, "習得") = 3000
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 30
	CASE 3
		ABLUP:GETNUM(PALAM, "習得") = 9000
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 50
	CASE 4
		ABLUP:GETNUM(PALAM, "習得") = 24000
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 100
	CASE 5
		ABLUP:GETNUM(PALAM, "習得") = 30000
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 200
	CASE 6
		ABLUP:GETNUM(PALAM, "習得") = 35000
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 300
	CASE 7
		ABLUP:GETNUM(PALAM, "習得") = 40000
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 400
	CASE 8
		ABLUP:GETNUM(PALAM, "習得") = 45000
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 500
	CASE 9
		ABLUP:GETNUM(PALAM, "習得") = 50000
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 600
	CASE IS >= 10
		LOCAL = ABL:撮影技能*ABL:撮影技能
		ABLUP:GETNUM(PALAM, "習得") = 600 * LOCAL
		ABLUPEXP:GETNUM(EXP, "撮影経験") = 7 * LOCAL
ENDSELECT

;習得早いもしくは好奇心
IF TALENT:習得早い || TALENT:好奇心
	TIMES ABLUP:GETNUM(PALAM, "習得") , 0.80
	TIMES ABLUPEXP:GETNUM(EXP, "撮影経験") , 0.80
ENDIF

;習得遅いもしくは無関心
IF TALENT:習得遅い || TALENT:無関心
	TIMES ABLUP:GETNUM(PALAM, "習得") , 1.30
	TIMES ABLUPEXP:GETNUM(EXP, "撮影経験") , 1.30
ENDIF

@CHECK_JUEL25
;習得の珠、撮影経験が必要
SIF JUEL:習得 >= ABLUP:GETNUM(PALAM, "習得") && EXP:撮影経験 >= ABLUPEXP:GETNUM(EXP, "撮影経験")
	RETURN 1

;上記の条件を満たせなかったら上げられない
RETURN 0

@ABLUP_MESSAGE25
SIF JUEL:習得 < ABLUP:GETNUM(PALAM, "習得")
	SETCOLOR 120, 120, 120
PRINTFORM [習得の珠×{ABLUP:GETNUM(PALAM, "習得")}]
RESETCOLOR

SIF EXP:撮影経験 < ABLUPEXP:GETNUM(EXP, "撮影経験")
	SETCOLOR 120, 120, 120
PRINTFORM [撮影経験{ABLUPEXP:GETNUM(EXP, "撮影経験")}以上]
RESETCOLOR

