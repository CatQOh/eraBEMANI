@EVENT_CHECK, イベント名, 対象 = -1
#DIMS DYNAMIC イベント名
#DIM DYNAMIC 対象

SIF 対象 == -1
	対象 = TARGET

;口上非表示フラグが立っていればDISABLE判定は無視する
SIF CFLAG:口上非表示
	GOTO 表示

TRYCCALLFORM DISABLE_EVENT_%イベント名%_K{NO:対象}
	SIF RESULT == 0
		RETURN 0
CATCH
ENDCATCH

$表示

TRYCALLFORM EVENT_%イベント名%

RETURN 1

