@ロケーション条件_キャノンボールサーキット
#DIM DYNAMIC LCOUNT
;出現条件のキャラをフルネーム(NAME)で入れる
{
#DIMS DYNAMIC 条件用キャラ = 
"ニクス",
"英利",
"筑波 美夏",
"英田 優里",
"菅生 彩香音",
"茂木 英美瑠",
"鈴鹿 亞希"
}

FOR LCOUNT, 0, VARSIZE("条件用キャラ")
	IF GETCHARANAME(条件用キャラ:LCOUNT) > 0
		SELECTCASE RELATION:GETCHARANAME(条件用キャラ:LCOUNT):(NO:PLAYER)
			;相性120%以上が条件
			CASE IS >= 120
				RETURN 1
		ENDSELECT
	ENDIF
NEXT

RETURN 0

@出現キャラ_キャノンボールサーキット
#DIM DYNAMIC LCOUNT

;NOではなく登録番号で指定する

FOR LCOUNT, 1, CHARANUM
	;学校などに行ってるキャラは居ない
	SIF CSTR:LCOUNT:現在地 != ""
		CONTINUE
	;出現率:LCOUNT = {出現率(100分率)} で指定できる
	SELECTCASE CHARANAMEF(LCOUNT)
		;レーサーなキャラがいる
		{
		CASE "ニクス", "英利", "筑波 美夏", "英田 優里", "菅生 彩香音", "茂木 英美瑠",
			 "鈴鹿 亞希", "ゼロワン：バンディング", "トヨタマヒメ", "獅子若", "ボニー"
		}
			出現率:LCOUNT = 50
	ENDSELECT
NEXT

	


