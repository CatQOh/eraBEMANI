@TRAIN_COMMENT, ARGS
#FUNCTIONS

SELECTCASE ARGS
	CASE "愛撫"
		LOCALS = %CALLNAME:TARGET%の体を撫で回します
	CASE "クンニ"
		LOCALS = %CALLNAME:TARGET%の女性器を舐め解します
	CASE "フェラする"
		LOCALS = %CALLNAME:TARGET%の男性器に口で奉仕します
	CASE "指挿入れ"
		LOCALS = %CALLNAME:TARGET%の女性器に指を挿入します
	CASE "アナル舐め"
		LOCALS = %CALLNAME:TARGET%の肛門を舐め解します
	CASE "アナル愛撫"
		LOCALS = %CALLNAME:TARGET%の肛門を指で弄ります
	CASE "胸揉み"
		LOCALS = %CALLNAME:TARGET%の乳房を揉みしだきます
	CASE "乳首責め"
		LOCALS = %CALLNAME:TARGET%の乳首を重点的に愛撫します
	CASE "胸愛撫"
		LOCALS = %CALLNAME:TARGET%の胸部を優しく撫で回します
	CASE "秘貝開帳"
		LOCALS = %CALLNAME:TARGET%に、自分で女性器を拡げるように命じます
	CASE "胸自慰"
		LOCALS = %CALLNAME:TARGET%に、胸を弄りながらの自慰を命じます
	CASE "自慰"
		LOCALS = %CALLNAME:TARGET%に、好きなように自慰をするように命じます
	CASE "足扱きする"
		LOCALS = %CALLNAME:TARGET%の男性器を足で踏んだり扱いたりします
	CASE "下着扱きする"
		LOCALS = %CALLNAME:PLAYER%が穿いてい\@ TEQUIP:PLAYER:下半身下着 ? る # た \@%CSTR:PLAYER:下半身下着%で、%CALLNAME:TARGET%の男性器を扱きます
	CASE "キスする"
		LOCALS = %CALLNAME:TARGET%とキスをします。場合によっては拒否される場合があります
	CASE "何もしない"
		LOCALS = 何もせずに%CALLNAME:TARGET%を放置します
	CASE "会話"
		LOCALS = %CALLNAME:TARGET%と他愛もない会話をします
	CASE "プレゼント"
		LOCALS = %CALLNAME:TARGET%に贈り物をします。物によってはお金が必要になります
	CASE "スキンシップ"
		LOCALS = %CALLNAME:TARGET%とじゃれ合います
	CASE "まったりする"
		LOCALS = %CALLNAME:TARGET%とまったりします
	CASE "家事"
		LOCALS = %CALLNAME:TARGET%と家の雑事を片付けます
	CASE "歌の練習"
		LOCALS = %CALLNAME:TARGET%の歌唱力を鍛えます
	CASE "罵倒"
		LOCALS = %CALLNAME:TARGET%にありとあらゆる罵詈雑言をぶつけます
	CASE "ビデオ鑑賞"
		LOCALS = 過去に撮影した%CALLNAME:TARGET%とのビデオを一緒に鑑賞します
	CASE "吸血する"
		LOCALS = %CALLNAME:TARGET%から血を吸います
	CASE "吸血させる"
		LOCALS = %CALLNAME:TARGET%に血を吸わせます。%CALLNAME:TARGET%は回復しますが、その調教中に一度しか行なえません
	CASE "お酒を飲ませる"
		LOCALS = %CALLNAME:TARGET%にお酒を飲ませます
	CASE "ローター"
		LOCALS = %CALLNAME:TARGET%の\@ PENIS(TARGET) ? 陰茎 # 陰核 \@を中心にローターで責め立てます
	CASE "電気按摩"
		LOCALS = %CALLNAME:TARGET%の\@ PENIS(TARGET) ? 陰茎 # 陰核 \@を中心に電気按摩で責め立てます
	CASE "クリキャップ"
		IF TEQUIP:クリキャップ
			LOCALS = %CALLNAME:TARGET%の陰核から振動機能付きクリキャップを取り外します
		ELSE
			LOCALS = %CALLNAME:TARGET%の陰核に振動機能付きクリキャップを取り付けます
		ENDIF
	CASE "オナホール"
		IF TEQUIP:オナホール
			LOCALS = %CALLNAME:TARGET%の陰茎からオナホールを取り外します
		ELSE
			LOCALS = %CALLNAME:TARGET%の陰茎にオナホールを被せます
		ENDIF
	CASE "バイブ"
		IF TEQUIP:バイブ
			LOCALS = %CALLNAME:TARGET%の膣からバイブを引き抜きます
		ELSE
			LOCALS = %CALLNAME:TARGET%の膣にバイブを挿入します
		ENDIF
	CASE "アナルバイブ"
		IF TEQUIP:アナルバイブ
			LOCALS = %CALLNAME:TARGET%の肛門からバイブを引き抜きます
		ELSE
			LOCALS = %CALLNAME:TARGET%の肛門にバイブを挿入します
		ENDIF
	CASE "アナルビーズ"
		IF TEQUIP:アナルビーズ
			LOCALS = %CALLNAME:TARGET%の肛門からアナルビーズを引き抜きます
		ELSE
			LOCALS = %CALLNAME:TARGET%肛門にアナルビーズを飲み込ませていきます
		ENDIF
	CASE "ニプルキャップ"
		IF TEQUIP:ニプルキャップ
			LOCALS = %CALLNAME:TARGET%の乳首からローターを取り外します
		ELSE
			LOCALS = %CALLNAME:TARGET%の乳首にローターを取り付けます
		ENDIF
	CASE "乳首ローター"
		LOCALS = %CALLNAME:TARGET%の乳首をローターで責め立てます
	CASE "搾乳器"
		IF TEQUIP:搾乳器
			LOCALS = %CALLNAME:TARGET%の乳房から搾乳器を取り外します
		ELSE
			LOCALS = 母乳を搾り取る器具を%CALLNAME:TARGET%の乳房に取り付けます
		ENDIF
	CASE "搾精コンドーム"
		IF TEQUIP:搾精コンドーム
			LOCALS = %CALLNAME:TARGET%のペニスから搾精用のコンドームを取り外します
		ELSE
			LOCALS = %CALLNAME:TARGET%のペニスに精液搾取用のコンドームを被せます
		ENDIF
	CASE "コンドーム"
		LOCALS = 避妊具を付けたり外したりします
	CASE "正常位"
		LOCALS = 正面から%CALLNAME:TARGET%の女性器に\@ PENIS(PLAYER) ? ペニス # ペニスバンド \@を挿入します
	CASE "後背位"
		LOCALS = 後ろから%CALLNAME:TARGET%の女性器に\@ PENIS(PLAYER) ? ペニス # ペニスバンド \@を挿入します
	CASE "騎乗位"
		LOCALS = %CALLNAME:TARGET%に、%CALLNAME:PLAYER%のペニスを自分から女性器で咥え込むように命じます
	CASE "対面座位"
		LOCALS = %CALLNAME:TARGET%と抱き合ったまま性交を行います
	CASE "背面座位"
		LOCALS = 後ろから%CALLNAME:TARGET%を抱き寄せて性交を行います
	CASE "正常位アナル"
		LOCALS = 正面から%CALLNAME:TARGET%の尻穴に\@ PENIS(PLAYER) ? ペニス # ペニスバンド \@を挿入します
	CASE "後背位アナル"
		LOCALS = 後ろから%CALLNAME:TARGET%の尻穴に\@ PENIS(PLAYER) ? ペニス # ペニスバンド \@を挿入します
	CASE "騎乗位アナル"
		LOCALS = %CALLNAME:TARGET%に、%CALLNAME:PLAYER%のペニスを自分から尻穴で咥え込むように命じます
	CASE "対面座位アナル"
		LOCALS = %CALLNAME:TARGET%と抱き合ったまま尻穴にペニスを挿入します
	CASE "背面座位アナル"
		LOCALS = 後ろから%CALLNAME:TARGET%を抱き寄せて尻穴にペニスを挿入します
	CASE "下着ずらし挿入"
		LOCALS = %CALLNAME:TARGET%が\@ TEQUIP:下半身下着 ? 穿いている%CSTR:下半身下着% # 着ている%CSTR:全身上着% \@をずらして女性器にペニスを挿入します
	CASE "下着ずらし挿入アナル"
		LOCALS = %CALLNAME:TARGET%が\@ TEQUIP:下半身下着 ? 穿いている%CSTR:下半身下着% # 着ている%CSTR:全身上着% \@をずらして尻穴にペニスを挿入します
	CASE "正常位させる"
		LOCALS = %CALLNAME:TARGET%に\@ PLAYER == MASTER ? 自分 # %CALLNAME:ASSI% \@を抱くようにと命じます
	CASE "後背位させる"
		LOCALS = %CALLNAME:TARGET%に\@ PLAYER == MASTER ? 自分 # %CALLNAME:ASSI% \@を後ろから犯すようにと命じます
	CASE "正常位アナルさせる"
		LOCALS = %CALLNAME:TARGET%のペニスを\@ PLAYER == MASTER ? 自分 # %CALLNAME:ASSI% \@の尻穴に挿入するようにと命じます
	CASE "後背位アナルさせる"
		LOCALS = %CALLNAME:TARGET%に\@ PLAYER == MASTER ? 自分 # %CALLNAME:ASSI% \@の尻穴を後ろから犯すようにと命じます
	CASE "逆レイプ"
		LOCALS = %CALLNAME:TARGET%のペニスを\@ VAGINA(PLAYER) ? 膣 # 尻穴 \@で強引に咥え込みます
	CASE "手淫"
		LOCALS = %CALLNAME:TARGET%に手で%CALLNAME:PLAYER%のペニスを扱くように命じます
	CASE "フェラチオ"
		LOCALS = %CALLNAME:TARGET%に口で%CALLNAME:PLAYER%のペニスに奉仕するように命じます
	CASE "パイズリ"
		LOCALS = %CALLNAME:TARGET%の乳房にペニスを挟んで扱き上げます
	CASE "素股"
		LOCALS = %CALLNAME:TARGET%の陰部に%CALLNAME:PLAYER%のペニスを擦りつけます
	CASE "泡踊り"
		LOCALS = 泡にまみれた%CALLNAME:TARGET%の全身で奉仕させます
	CASE "足扱き"
		LOCALS = %CALLNAME:TARGET%の足で%CALLNAME:PLAYER%のペニスを扱くように命じます
	CASE "足を舐めさせる"
		LOCALS = %CALLNAME:TARGET%に%CALLNAME:PLAYER%の足を舐めさせ権威を示します
	CASE "縦パイズリ"
		LOCALS = 正面から%CALLNAME:TARGET%の乳房の間にペニスを埋めます
	CASE "パイズリフェラ"
		LOCALS = %CALLNAME:TARGET%の乳房でペニスを挟みながら同時に口で奉仕するように命じます
	CASE "母乳飲み"
		LOCALS = %CALLNAME:TARGET%の母乳を飲みます
	CASE "兜合わせ"
		LOCALS = %CALLNAME:TARGET%と%CALLNAME:PLAYER%のペニスを擦り合わせます
	CASE "下着扱き"
		LOCALS = %CALLNAME:TARGET%が穿いてい\@ TEQUIP:下半身下着 ? る # た \@%CSTR:下半身下着%で%CALLNAME:PLAYER%のペニスを扱かせます
	CASE "オナホ扱き"
		LOCALS = %CALLNAME:TARGET%にオナホで%CALLNAME:PLAYER%のペニスを扱かせます
	CASE "スパンキング"
		LOCALS = %CALLNAME:TARGET%の尻を何度も叩きます
	CASE "乳スパンキング"
		LOCALS = %CALLNAME:TARGET%の胸を軽く叩きます
	CASE "鞭"
		LOCALS = %CALLNAME:TARGET%を鞭で殴打します
	CASE "蝋燭"
		LOCALS = %CALLNAME:TARGET%の体に熱した蝋を垂らします
	CASE "針"
		LOCALS = %CALLNAME:TARGET%の陰部を針で責め立てます
	CASE "アイマスク"
		IF TEQUIP:アイマスク
			LOCALS = %CALLNAME:TARGET%からアイマスクを外します
		ELSE
			LOCALS = %CALLNAME:TARGET%の視界をアイマスクで覆います
		ENDIF
	CASE "縄"
		IF TEQUIP:縄
			LOCALS = %CALLNAME:TARGET%の縄を解きます
		ELSE
			LOCALS = %CALLNAME:TARGET%を縄で縛り付けます
		ENDIF
	CASE "ボールギャグ"
		IF TEQUIP:ボールギャグ
			LOCALS = %CALLNAME:TARGET%から轡を外します
		ELSE
			LOCALS = %CALLNAME:TARGET%の口に轡を嵌めて喋られないようにします
		ENDIF
	CASE "腹パン"
		LOCALS = %CALLNAME:TARGET%の腹部を思い切り殴ります
	CASE "踏む"
		LOCALS = %CALLNAME:TARGET%のいろんな部位を踏みます
	CASE "裸締め"
		LOCALS = %CALLNAME:TARGET%を後ろから羽交い締めにします。体力次第で気絶する可能性があります
	CASE "首を絞める"
		LOCALS = 死なない程度に%CALLNAME:TARGET%の首を締めます
	CASE "ビンタ"
		LOCALS = %CALLNAME:TARGET%の頬を平手打ちします
	CASE "異物挿入"
		LOCALS = %CALLNAME:TARGET%の\@ VAGINA(TARGET) ? 女性器、もしくは #  \@肛門に様々な異物を挿入します
	CASE "異物挿入解除"
		LOCALS = %CALLNAME:TARGET%の\@ VAGINA(TARGET) ? 女性器、もしくは #  \@肛門に挿入した異物を引き抜きます
	CASE "クンニさせる"
		LOCALS = %CALLNAME:TARGET%に、%CALLNAME:PLAYER%の女性器に口で奉仕するように命じます
	CASE "貝あわせ"
		LOCALS = %CALLNAME:TARGET%と互いの女性器を擦り付け合います
	CASE "助手を犯させる"
		LOCALS = %CALLNAME:TARGET%に%CALLNAME:ASSI%を犯させます
	CASE "顔面騎乗する"
		LOCALS = %CALLNAME:TARGET%の顔面に跨って無理やり口で奉仕させます
	CASE "ダブルフェラ"
		LOCALS = %CALLNAME:TARGET%と%CALLNAME:ASSI%の二人同時のフェラチオを楽しみます
	CASE "ダブルパイズリ"
		LOCALS = %CALLNAME:TARGET%と%CALLNAME:ASSI%の二人同時のパイズリを楽しみます
	CASE "乳の揉み合い"
		LOCALS = %CALLNAME:TARGET%と互いの乳房を揉み合います
	CASE "イラマチオ"
		LOCALS = %CALLNAME:TARGET%の喉奥までペニスを突き入れます
	CASE "フィストファック"
		LOCALS = %CALLNAME:TARGET%の女性器に拳を挿入します
	CASE "アナルフィスト"
		LOCALS = %CALLNAME:TARGET%のアナルに拳を挿入します
	CASE "両穴フィスト"
		LOCALS = %CALLNAME:TARGET%と女性器とアナルに拳を挿入します
	CASE "放尿"
		LOCALS = %CALLNAME:TARGET%にその場で放尿させます
	CASE "アナルを舐めさせる"
		LOCALS = %CALLNAME:TARGET%に%CALLNAME:PLAYER%の肛門を舐めさせます
	CASE "浣腸器"
		IF TEQUIP:浣腸器
			LOCALS = %CALLNAME:TARGET%のアナルプラグを外して排便させます
		ELSE
			LOCALS = %CALLNAME:TARGET%の腸内に浣腸液を流し込んで栓をします
		ENDIF
	CASE "空気浣腸"
		LOCALS = %CALLNAME:TARGET%の尻穴に空気を流し込みます
	CASE "拡張バルーン"
		IF TEQUIP:拡張バルーン
			LOCALS = %CALLNAME:TARGET%の尻穴を拡げるバルーンを萎めます
		ELSE
			LOCALS = %CALLNAME:TARGET%の尻穴をバルーンで拡張します
		ENDIF
	CASE "アナル電極"
		IF TEQUIP:アナル電極
			LOCALS = %CALLNAME:TARGET%のアナルから電極付きプラグを引き抜きます
		ELSE
			LOCALS = 電気が流れるプラグを%CALLNAME:TARGET%のアナルに挿入します
		ENDIF
	CASE "乳房電極"
		IF TEQUIP:乳房電極
			LOCALS = %CALLNAME:TARGET%の乳首から電極付きニップレスを取り外します
		ELSE
			LOCALS = 電気が流れるニップレスを%CALLNAME:TARGET%の乳首に取り付けます
		ENDIF
	CASE "乳ファック"
		LOCALS = %CALLNAME:TARGET%の胸部で強引にペニスを扱き上げます
	CASE "触手召喚"
		IF TEQUIP:触手
			LOCALS = 触手を送り返します
		ELSE
			LOCALS = 触手を召喚します
		ENDIF
	CASE "触手挿入"
		IF TEQUIP:バイブ
			LOCALS = %CALLNAME:TARGET%の膣内から触手を引き抜きます
		ELSE
			LOCALS = %CALLNAME:TARGET%の膣内を触手で蹂躙します
		ENDIF
	CASE "アナル触手"
		IF TEQUIP:アナルバイブ
			LOCALS = %CALLNAME:TARGET%の尻穴から触手を引き抜きます
		ELSE
			LOCALS = %CALLNAME:TARGET%の腸内を触手で蹂躙します
		ENDIF
	CASE "触手クリ責め"
		IF TEQUIP:クリキャップ
			LOCALS = %CALLNAME:TARGET%の陰核に吸い付く触手にやめるよう指示します
		ELSE
			LOCALS = 触手に%CALLNAME:TARGET%の陰核を弄り倒すように指示します
		ENDIF
	CASE "触手乳首責め"
		IF TEQUIP:ニプルキャップ
			LOCALS = %CALLNAME:TARGET%の乳首に吸い付く触手にやめるよう指示します
		ELSE
			LOCALS = 触手に%CALLNAME:TARGET%の乳首を弄り倒すように指示します
		ENDIF
	CASE "触手搾乳"
		IF TEQUIP:搾乳器
			LOCALS = %CALLNAME:TARGET%の乳搾りを終えます
		ELSE
			LOCALS = 触手で%CALLNAME:TARGET%の母乳を搾ります
		ENDIF
	CASE "触手緊縛"
		IF TEQUIP:縄
			LOCALS = %CALLNAME:TARGET%を縛る触手を解きます
		ELSE
			LOCALS = 触手で%CALLNAME:TARGET%の体を拘束します
		ENDIF
	CASE "触手浣腸"
		IF TEQUIP:浣腸器
			LOCALS = %CALLNAME:TARGET%の尻穴から触手を引き抜いて排便させます
		ELSE
			LOCALS = 触手から直に%CALLNAME:TARGET%の腸内へと浣腸液を流し込みます
		ENDIF
	CASE "触手口辱"
		IF TEQUIP:触手口辱
			LOCALS = %CALLNAME:TARGET%の口から触手を引き抜きます
		ELSE
			LOCALS = %CALLNAME:TARGET%の口を触手で塞ぎます
		ENDIF
	CASE "触手ペニス責め"
		IF TEQUIP:オナホール
			LOCALS = %CALLNAME:TARGET%のペニスに吸い付く触手にやめるよう指示します
		ELSE
			LOCALS = 触手で%CALLNAME:TARGET%のペニスを咥え込みます
		ENDIF
	CASE "触手搾精"
		IF TEQUIP:搾精コンドーム
			LOCALS = %CALLNAME:TARGET%のペニスに吸い付く触手にやめるよう指示します
		ELSE
			LOCALS = 触手で%CALLNAME:TARGET%の精液を搾り取ります
		ENDIF
	CASE "ローション"
		LOCALS = %CALLNAME:TARGET%の体にローションを塗りたくります
	CASE "媚薬"
		LOCALS = %CALLNAME:TARGET%に媚薬を飲ませます
	CASE "魅惑の粘液"
		LOCALS = %CALLNAME:TARGET%の体に甘い香りのする謎の粘液を塗りつけます
	CASE "利尿剤"
		LOCALS = %CALLNAME:TARGET%に利尿剤を飲ませます
	CASE "ビデオカメラ"
		IF TEQUIP:ビデオカメラ
			LOCALS = 撮影を終えます
		ELSE
			LOCALS = %CALLNAME:TARGET%の痴態を撮影します
		ENDIF
	CASE "野外プレイ"
		IF TEQUIP:野外プレイ
			LOCALS = 家に帰ります
		ELSE
			LOCALS = %CALLNAME:TARGET%に首輪を付けて外に連れ出します
		ENDIF
	CASE "粘液ぶっかけ"
		LOCALS = 溜め込んでいた保管済み粘液を%CALLNAME:TARGET%に浴びせます
	CASE "助手を犯す"
		LOCALS = %CALLNAME:TARGET%の目の前で%CALLNAME:ASSI%を犯します
	CASE "羞恥プレイ"
		IF TEQUIP:羞恥プレイ
			LOCALS = 普通の部屋に戻ります
		ELSE
			LOCALS = %CALLNAME:TARGET%を大きな鏡のある部屋へと連れて行きます
		ENDIF
	CASE "お風呂場プレイ"
		IF TEQUIP:お風呂場
			LOCALS = お風呂を上がります
		ELSE
			LOCALS = %CALLNAME:TARGET%をお風呂場へと連れて行きます
		ENDIF
	CASE "シャワー"
		IF TEQUIP:シャワー
			LOCALS = シャワーを止めます
		ELSE
			LOCALS = %CALLNAME:TARGET%にシャワーを浴びせます
		ENDIF
	CASE "新妻プレイ"
		IF TEQUIP:エプロン
			LOCALS = 新婚ごっこを終えます
		ELSE
			LOCALS = %CALLNAME:TARGET%にエプロンを着せます
		ENDIF
	CASE "写真撮影"
		LOCALS = 直前の行為を撮影します
	CASE "強精神薬"
		LOCALS = %CALLNAME:TARGET%に絶頂を止める薬を飲ませます
	CASE "コンドーム精飲（奴隷）"
		LOCALS = 避妊具に溜まった精液を%CALLNAME:TARGET%に飲ませます
	CASE "コンドーム精飲（助手）"
		LOCALS = 避妊具に溜まった精液を%CALLNAME:ASSI%に飲ませます
	CASE "ローション注入"
		LOCALS = %CALLNAME:TARGET%のアナルに直接ローションを注入します
	CASE "キス（ドラゴン）"
		LOCALS = %CSTR:PLAYER:ドラゴン%に%CALLNAME:TARGET%とキスをさせます
	CASE "フェラさせる（ドラゴン）"
		LOCALS = %CSTR:PLAYER:ドラゴン%のペニスを%CALLNAME:TARGET%に舐めさせます
	CASE "舐め回す（ドラゴン）"
		LOCALS = %CSTR:PLAYER:ドラゴン%の大きな舌で%CALLNAME:TARGET%を舐め回します
	CASE "睨みつける（ドラゴン）"
		LOCALS = %CSTR:PLAYER:ドラゴン%の瞳で%CALLNAME:TARGET%を威嚇します
	CASE "膣を犯す（ドラゴン）"
		LOCALS = %CSTR:PLAYER:ドラゴン%に%CALLNAME:TARGET%の女性器を犯させます
	CASE "アナルを犯す（ドラゴン）"
		LOCALS = %CSTR:PLAYER:ドラゴン%に%CALLNAME:TARGET%のアナルを犯させます
	CASE "手淫する"
		LOCALS = %CALLNAME:TARGET%のペニスを手で扱きます
	CASE "自慰見せつけ"
		LOCALS = %CALLNAME:TARGET%に自慰を見せつけます
	CASE "秘貝開帳見せつけ"
		LOCALS = %CALLNAME:TARGET%に、自らの膣口を拡げて見せつけます
	CASE "キスしてもらう"
		LOCALS = %CALLNAME:TARGET%にキスをさせます
	CASE "情欲を煽る"
		LOCALS = 言葉や仕草で%CALLNAME:TARGET%の情欲を煽ります
	CASE "バイブ被挿入"
		LOCALS = %CALLNAME:TARGET%に膣をバイブで責めてもらいます
	CASE "アナルバイブ被挿入"
		LOCALS = %CALLNAME:TARGET%にアナルをバイブで責めてもらいます
	CASE "アナルバイブ被挿入"
		LOCALS = %CALLNAME:TARGET%に電気按摩で責めてもらいます
	CASE "鞭打ちしてもらう"
		LOCALS = %CALLNAME:TARGET%に鞭で叩いてもらいます
	CASE "踏んでもらう"
		LOCALS = %CALLNAME:TARGET%に踏んでもらいます
	CASE "叩いてもらう"
		LOCALS = %CALLNAME:TARGET%に叩いてもらいます
	CASE "素股する"
		LOCALS = %CALLNAME:TARGET%のペニスを%CALLNAME:PLAYER%の股間で擦り上げます
	CASE "パイズリする"
		LOCALS = %CALLNAME:TARGET%のペニスを胸で包んで扱き上げます
	CASE "パイズリフェラする"
		LOCALS = %CALLNAME:TARGET%のペニスを胸で包んで扱き上げ、同時に口も使って責め立てます
	CASE "フィストファックさせる"
		LOCALS = %CALLNAME:TARGET%の腕で膣を犯してもらいます
	CASE "アナルフィストさせる"
		LOCALS = %CALLNAME:TARGET%の腕でアナルを犯してもらいます
	CASE "触手射精止め"
		LOCALS = 細い触手で%CALLNAME:TARGET%のペニスを強く締め付け、射精できないようにします
	CASE "触手強制搾精"
		LOCALS = 細い触手で%CALLNAME:TARGET%の尿道から直接粘液を流し込み、擬似的な射精の快感を与えます
	CASE "触手強制放尿"
		LOCALS = 細い触手で%CALLNAME:TARGET%の尿道から直接液体を流し込み、擬似的に放尿させます
	CASE "腸洗浄"
		LOCALS = %CALLNAME:TARGET%の腸内をぬるま湯で洗って綺麗にします
	CASE "ゲームセンター"
		IF TEQUIP:ゲームセンター
			LOCALS = %GC店名%を後にして自宅に帰ります
		ELSE
			LOCALS = %CALLNAME:TARGET%と一緒に%GC店名%に行きます
		ENDIF
	CASE "トイレに行く"
		IF TEQUIP:トイレ
			LOCALS = トイレを出てフロアに戻ります
		ELSE
			LOCALS = %CALLNAME:TARGET%をトイレに連れ込みます
		ENDIF
	CASE "IIDXをプレイする"
		LOCALS = %CALLNAME:TARGET%と一緒にbeatmaniaIIDXをプレイします
	CASE "ポップンをプレイする"
		LOCALS = %CALLNAME:TARGET%と一緒にポップンミュージックで遊びます
	CASE "DDRをプレイする"
		LOCALS = %CALLNAME:TARGET%と一緒にDance Dance Revolutionをプレイします
	CASE "ギタドラをプレイする"
		LOCALS = %CALLNAME:TARGET%\@ ASSI > 0 ? 、%CALLNAME:ASSI% #  \@と一緒にギタドラでセッションします
	CASE "jubeatをプレイする"
		LOCALS = %CALLNAME:TARGET%と一緒にjubeatで遊びます
	CASE "リフレクをプレイする"
		LOCALS = %CALLNAME:TARGET%と一緒にリフレクビートで遊びます
	CASE "ボルテをプレイする"
		LOCALS = %CALLNAME:TARGET%と一緒にSOUND VOLTEXで遊びます
	CASE "ダンエボをプレイする"
		LOCALS = %CALLNAME:TARGET%と一緒にDance Evolutionで踊ります
	CASE "魔法の練習"
		IF ABL:PLAYER:魔法技能 && ABL:魔法技能
			LOCALS = %CALLNAME:TARGET%と一緒に魔法の練習をします
		ELSEIF ABL:PLAYER:魔法技能
			LOCALS = %CALLNAME:TARGET%を相手に魔法の練習をします
		ELSEIF ABL:魔法技能
			LOCALS = %CALLNAME:TARGET%に魔法の練習をさせます
		ENDIF
	CASE "魔法詠唱"
		LOCALS = 魔力を消費して魔法を詠唱します
	CASE "料理を作る"
		LOCALS = %CALLNAME:TARGET%に料理を作らせます
	CASE "お菓子を作る"
		LOCALS = %CALLNAME:TARGET%にお菓子を作らせます
	CASE "楽器の練習"
		LOCALS = %CALLNAME:TARGET%に楽器演奏の練習をさせます
	CASE "機械の勉強"
		LOCALS = %CALLNAME:TARGET%に電子機械についての指導をします
	CASE "プログラミング演習"
		LOCALS = %CALLNAME:TARGET%に、さらに専門的な機械の指導をします
	CASE "尻尾を撫でる"
		LOCALS = %CALLNAME:TARGET%の尻尾をもふもふします
	CASE "ハッキング"
		LOCALS = %CALLNAME:TARGET%をハッキングして特殊な命令を送ります
	CASE "アナル愛撫させる"
		LOCALS = %CALLNAME:TARGET%に%CALLNAME:PLAYER%のアナルを愛撫させます
	CASE "ペニスバンド"
		LOCALS = ペニスバンドを装着します
	CASE "電気按摩愛撫させる"
		LOCALS = %CALLNAME:TARGET%に電気按摩で%CALLNAME:PLAYER%を愛撫させます
	CASE "QMAをプレイする"
		LOCALS = %CALLNAME:TARGET%と一緒にクイズマジックアカデミーで遊びます
	CASE "maimaiをプレイする"
		LOCALS = %CALLNAME:TARGET%と一緒にmaimaiで遊びます
	CASE "CHUNITHMをプレイする"
		LOCALS = %CALLNAME:TARGET%と一緒にCHUNITHMで遊びます
	CASE "ビデオポーカーで遊ぶ"
		LOCALS = ビデオポーカーで遊びます
	CASE "ポーカーソリティアで遊ぶ"
		LOCALS = ポーカーソリティアで遊びます
	CASE "モンテカルロで遊ぶ"
		LOCALS = モンテカルロで遊びます
	CASE "ブラックジャックで遊ぶ"
		LOCALS = ブラックジャックで遊びます
	CASE "ミッドナイトスペシャル"
		LOCALS = ミッドナイトスペシャル(精力剤)を使います
	CASE "排卵マッサージ"
		LOCALS = 子宮周辺などをマッサージして排卵や着床までの流れがスムーズに行えるようにします
	CASE "電動エネマグラ"
		IF TEQUIP:電動エネマグラ
			LOCALS = アナルから電動エネマグラを引き抜きます
		ELSE
			LOCALS = 前立腺の刺激に特化したエネマグラをアナルに挿入します
		ENDIF
	CASE "搾精器"
		IF TEQUIP:搾精器
			LOCALS = ペニスから搾精器を取り外します
		ELSE
			LOCALS = 精液を搾り取るための搾精器をペニスに装着します
		ENDIF
	CASE "射精寸止め"
		LOCALS = %CALLNAME:TARGET%が射精する寸前で止めます
	CASE "貞操帯"
		IF CFLAG:貞操帯
			LOCALS = 貞操帯を外します
		ELSE
			LOCALS = 貞操帯を装着します
		ENDIF
	CASE "女装させる"
		IF TEQUIP:女装
			LOCALS = 女装をやめさせ、元の服装に戻します
		ELSE
			LOCALS = %CALLNAME:TARGET%に女装をさせます
		ENDIF
	CASE "尿道カテーテル"
		IF TEQUIP:尿道カテーテル
			LOCALS = 尿道にカテーテルを挿入します
		ELSE
			LOCALS = 尿道からカテーテルを引き抜きます
		ENDIF
	CASE "ふたなり化"
		LOCALS = スライムキャラをふたなり化したりふたなり解除します
	CASE "ローター愛撫させる"
		LOCALS = %CALLNAME:TARGET%にローターで愛撫させます
	CASE "電気按摩愛撫させる"
		LOCALS = %CALLNAME:TARGET%に電気按摩で愛撫させます
	CASE "ペットに舐めさせる"
		LOCALS = %CALLNAME:ASSI%に%CALLNAME:TARGET%の体を舐めさせます
	CASE "ペットに犯させる"
		LOCALS = %CALLNAME:TARGET%に%CALLNAME:ASSI%をけしかけます
	CASEELSE
		LOCALS = 
ENDSELECT

RETURNF LOCALS

