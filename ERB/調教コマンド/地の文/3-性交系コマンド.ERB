;-------------------------------------------------
;正常位
;-------------------------------------------------
@MESSAGE_COM_正常位
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	;前回コマンド
	SELECTCASE PREVCOMS
		CASE "正常位"
			;調教者がペニス持ち
			IF PENIS(PLAYER)
				PRINTFORMW %CALLNAME:PLAYER%は%CALLNAME:TARGET%の膣内の感触を味わいながら、再び腰を動かし始めた……
			;ペニスバンドを使用している場合
			ELSE
				PRINTFORMW %CALLNAME:PLAYER%は%CALLNAME:TARGET%の膣内からペニスバンドを抜かずに、再び腰を動かし始めた……
			ENDIF
			RETURN 0
		CASE "後背位", "騎乗位", "対面座位", "背面座位"
			PRINTFORMW %CALLNAME:PLAYER%は膣内から抜き去ることなく%CALLNAME:TARGET%を仰向けに寝かせ、再び腰を動かし始めた……
			RETURN 0
	ENDSELECT
ENDIF

SIF 上級売春中
	PRINTFORML %CALLNAME:TARGET%はおもむろに仰向けになっては、女の快感を求めてくる
PRINTFORM %CALLNAME:PLAYER%は%CALLNAME:TARGET%の
;着衣中
IF TEQUIP:下半身上着 || (TEQUIP:全身上着 && CFLAG:着衣併用 != 1)
	IF CFLAG:挿入可
		PRINTFORM %OVER_GENITAL(TARGET)%の中に手を忍ばせ、\@ TEQUIP:下半身下着 ? %CSTR:下半身下着%をずらして、# 裾を捲り上げ、 \@その露わになった
	ELSE
		PRINTFORM %OVER_GENITAL(TARGET)%\@ TEQUIP:下半身下着 ? と%CSTR:下半身下着% #  \@を脱がして、その
	ENDIF
ELSEIF TEQUIP:下半身下着
	PRINTFORM %CSTR:下半身下着%をずらして、無防備になった
;潤滑LV4以上
ELSEIF PALAM:潤滑 >= PALAMLV:4
	PRINT 濡れそぼった
ENDIF
;処女
SIF TALENT:処女
	PRINT 男を知らない
;ケモ
IF TALENT:ファーリー
	SELECTCASE RAND:3
		CASE 0
			PRINT 毛むくじゃらの割れ目を
		CASE 1
			PRINT 秘部の体毛を先端で掻き分け
		CASE 2
			PRINT 体毛に覆われる女性器をこじ開け
	ENDSELECT
;無機物
ELSEIF TALENT:無機物
	SELECTCASE RAND:2
		CASE 0
			PRINT 無機質な膣口を指で拡げて
		CASE 1
			PRINT 形だけの生殖器を
	ENDSELECT
;小柄体型
ELSEIF TALENT:小柄体型
	SELECTCASE RAND:3
		CASE 0
			PRINT 幼い割れ目を
		CASE 1
			PRINT 未成熟な秘裂を
		CASE 2
			PRINT まだ毛も生えていない膣口を押し広げ
	ENDSELECT
ELSE
	SELECTCASE RAND:3
		CASE 0
			PRINT 秘裂を
		CASE 1
			PRINT 膣口に先端をあてがい
		CASE 2
			PRINT 柔らかな秘肉をこじ開け
	ENDSELECT
ENDIF
IF TALENT:アニマル
	PRINTL 、後ろ足を掴みながら貫いた……
	SIF CFLAG:依存度 >= 1500
		PRINTFORML %CALLNAME:TARGET%は嬉しそうな鳴き声を上げ、舌を出して飼い主の%CALLNAME:PLAYER%に媚びている……
	WAIT
;陥落済み
ELSEIF CFLAG:陥落
	PRINTL 貫いた
	PRINTFORMW %CALLNAME:TARGET%も%CALLNAME:PLAYER%に足を絡め腰を押し付けてくる……
ELSE
	PRINTW 貫いた……
ENDIF

;-------------------------------------------------
;後背位
;-------------------------------------------------
@MESSAGE_COM_後背位
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	;前回コマンド
	SELECTCASE PREVCOMS
		CASE "後背位"
			;調教者がペニス持ち
			IF PENIS(PLAYER)
				PRINTFORMW %CALLNAME:TARGET%の膣内の感触を味わいながら、再び腰を動かし始めた……
			;ペニスバンドを使用している場合
			ELSE
				PRINTFORMW %CALLNAME:TARGET%の膣内から抜かずに、再び腰を動かし始めた……
			ENDIF
			RETURN 0
		CASE "正常位"
			PRINTFORMW 膣内から抜き去ることなく%CALLNAME:TARGET%の後ろに回り、再び腰を動かし始めた……
			RETURN 0
		CASE "対面座位", "背面座位", "騎乗位"
			PRINTFORMW 挿入したまま%CALLNAME:TARGET%を寝かせて四つん這いにし、再び腰を動かし始めた……
			RETURN 0
	ENDSELECT
ENDIF

SIF 上級売春中
	PRINTFORML %CALLNAME:TARGET%の要望は自らも得たいというもので、秘部を弄り%CALLNAME:PLAYER%を誘っている
IF TEQUIP:ゲームセンター
	PRINTFORM %CALLNAME:TARGET%を前かがみにさせ、
ELSE
	PRINTFORM %CALLNAME:TARGET%を四つん這いにさせ、
ENDIF
;着衣中
IF TEQUIP:下半身上着 || (TEQUIP:全身上着 && CFLAG:着衣併用 != 1)
	IF CFLAG:挿入可
		PRINTFORM %OVER_GENITAL(TARGET)%に隠れた\@ TEQUIP:下半身下着 ? %CSTR:下半身下着%をずらして、# 秘部を暴き、 \@その露わになった
	ELSE
		PRINTFORM %OVER_GENITAL(TARGET)%\@ TEQUIP:下半身下着 ? と%CSTR:下半身下着% #  \@を脱がして、その
	ENDIF
ELSEIF TEQUIP:下半身下着
	PRINTFORM %CSTR:下半身下着%をずらして、無防備になった
;潤滑LV4以上
ELSEIF PALAM:潤滑 >= PALAMLV:4
	PRINT 濡れそぼった
ENDIF
;処女
SIF TALENT:処女
	PRINT 男を知らない
;ケモ
IF TALENT:ファーリー
	SELECTCASE RAND:3
		CASE 0
			PRINT 毛むくじゃらの割れ目を
		CASE 1
			PRINT 秘部の体毛を先端で掻き分け
		CASE 2
			PRINT 体毛に覆われる女性器をこじ開け
	ENDSELECT
;無機物
ELSEIF TALENT:無機物
	SELECTCASE RAND:2
		CASE 0
			PRINT 無機質な膣口を指で拡げて
		CASE 1
			PRINT 形だけの生殖器の
	ENDSELECT
;小柄体型
ELSEIF TALENT:小柄体型
	SELECTCASE RAND:3
		CASE 0
			PRINT 幼い割れ目の
		CASE 1
			PRINT 未成熟な秘裂の
		CASE 2
			PRINT まだ毛も生えていない膣口を押し広げ
	ENDSELECT
ELSE
	SELECTCASE RAND:3
		CASE 0
			PRINT 秘裂の
		CASE 1
			PRINT 膣口に先端をあてがい
		CASE 2
			PRINT 柔らかな秘肉をこじ開け
	ENDSELECT
ENDIF
IF (CFLAG:陥落 && !TEQUIP:ゲームセンター) || (TALENT:アニマル && CFLAG:依存度 >= 1500)
	PRINTFORML 最奥まで突き入れた
	IF TALENT:尻尾
		PRINTFORML %CALLNAME:TARGET%は激しく尻尾を振りながら%CALLNAME:PLAYER%との交尾を悦んでいる……
	ELSE
		PRINTFORML %CALLNAME:TARGET%は尻穴をひくつかせながらさらに深くまで%CALLNAME:PLAYER%を誘い込んでいる……
	ENDIF
ELSE
	PRINTFORML 最奥まで突き入れた……
ENDIF
IF TEQUIP:ゲームセンター
	PRINTFORM %CALLNAME:TARGET%は壁に手をついて、
	IF ABL:Ｖ感覚 >= 5
		PRINTL 立っているのもままらないほどの快楽に膝をがくつかせている……
	ELSE
		PRINTL 外に漏れないよう必死に嬌声を堪えている……
	ENDIF
ENDIF
WAIT

;-------------------------------------------------
;騎乗位
;-------------------------------------------------
@MESSAGE_COM_騎乗位
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	;前回コマンド
	SELECTCASE PREVCOMS
		CASE "騎乗位"
			;調教者がペニス持ち
			IF PENIS(PLAYER)
				PRINTFORMW %CALLNAME:TARGET%は挿入されたままの%CALLNAME:PLAYER%のペニスを締め付け、再び腰を動かし始めた……
			;ペニバン使ってる場合
			ELSE
				PRINTFORMW %CALLNAME:TARGET%は挿入されたままのペニスバンドを締め付け、再び腰を動かし始めた……
			ENDIF
			RETURN 0
		CASE "正常位", "後背位"
			PRINTFORMW 膣内から抜き去ることなく%CALLNAME:TARGET%を抱き上げ、下から腰を突き上げた……
			RETURN 0
		CASE "対面座位", "背面座位"
			PRINTFORMW %CALLNAME:PLAYER%は挿入したまま%CALLNAME:TARGET%を上に載せる形で横になり、下から腰を突き上げた……
			RETURN 0
	ENDSELECT
ENDIF

PRINTFORM %CALLNAME:TARGET%は
SIF 上級売春中 || FALLTYPE(TARGET) == 2
	PRINTFORM %CALLNAME:PLAYER%を押し倒して跨ると、舌なめずりしながら
IF OVER_GENITAL(TARGET) != ""
	IF CFLAG:挿入可
		SIF TEQUIP:下半身下着
			PRINTFORM %CSTR:下半身下着%を降ろし、
		SIF TEQUIP:下半身上着 || TEQUIP:全身上着
			PRINTFORM %OVER_GENITAL(TARGET)%をたくし上げ
	ELSE
		SIF TEQUIP:下半身上着 || TEQUIP:全身上着
			PRINTFORM %OVER_GENITAL(TARGET)%
		SIF TEQUIP:下半身下着 && (TEQUIP:下半身上着 || TEQUIP:全身上着)
			PRINT と
		SIF TEQUIP:下半身下着
			PRINTFORM %CSTR:下半身下着%
		PRINT を脱いで
	ENDIF
ENDIF
IF 上級売春中 || FALLTYPE(TARGET) == 2
	PRINT これ見よがしに
ELSE
	PRINTFORM %CALLNAME:PLAYER%の上にまたがり、ゆっくりと腰を下ろし、
ENDIF

;処女
SIF TALENT:処女
	PRINT 男を知らない
SIF TALENT:ファーリー
	PRINT 毛むくじゃらの
SIF TALENT:小柄体型 && !TALENT:無機物
	PRINT 幼い
PRINTL 女性器でペニスを飲み込んでいった……

;陥落済み
IF CFLAG:陥落
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%のペニスをぎゅっと締め上げ、
	;小柄体型
	SIF TALENT:小柄体型
		PRINT 抑えられない欲望を満たそうと小さな身体で懸命に
	SELECTCASE TALENT:バストサイズ
		;巨乳
		CASE 4
			PRINT 大きく胸を揺らしながら
		;爆乳
		CASE 5
			PRINT 跳ねる胸に身体ごと揺らされながら
	ENDSELECT
	PRINTL 自ら腰を動かしている……
ENDIF
WAIT

;-------------------------------------------------
;対面座位
;-------------------------------------------------
@MESSAGE_COM_対面座位
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	;前回コマンド
	SELECTCASE PREVCOMS
		CASE "対面座位"
			PRINTFORMW %CALLNAME:PLAYER%は%CALLNAME:TARGET%の体を離さずに、腰を動かし続けた……
			RETURN 0
		CASE "正常位", "騎乗位"
			PRINTFORM 膣内からペニスを抜き去ることなく%CALLNAME:TARGET%の身体を
			;前回の調教が正常位
			IF PREVCOMS == "正常位"
				PRINT 抱き上げ、
			ELSE
				PRINT ぎゅっと抱き寄せ、
			ENDIF
			PRINTW 腰を動かし続けた……
			RETURN 0
		CASE "後背位", "背面座位"
			PRINTFORM 挿入したまま身体をこちらに向かせ
			;前回の調教が後背位
			IF PREVCOMS == "後背位"
				PRINT て抱き上げ、
			ELSE
				PRINT るとぎゅっと抱き寄せ、
			ENDIF
			PRINTW 腰を動かし続けた……
			RETURN 0
	ENDSELECT
ENDIF

IF 上級売春中
	PRINTFORML %CALLNAME:TARGET%が膝立ちで%CALLNAME:PLAYER%に手を回し、顔を覗き込みながら%CALLNAME:PLAYER%にペニスに触れる
	PRINTFORM 十分に勃ってること確認した%CALLNAME:TARGET%はゆっくりと腰を降ろし、
	;潤滑LV4以上
	SIF PALAM:潤滑 >= PALAMLV:4
		PRINT 濡れそぼつ
	;処女
	SIF TALENT:処女
		PRINT 男を知らない
	;ケモ
	IF TALENT:ファーリー
		SELECTCASE RAND:3
			CASE 0
				PRINT 毛むくじゃらの割れ目で
			CASE 1
				PRINT 体毛を指でで掻き分けた秘部で
			CASE 2
				PRINT 体毛に覆われる女性器で
		ENDSELECT
	;無機物
	ELSEIF TALENT:無機物
		SELECTCASE RAND:2
			CASE 0
				PRINT 無機質な膣口を指で拡げて
			CASE 1
				PRINT 形だけの生殖器で
		ENDSELECT
	;小柄体型
	ELSEIF TALENT:小柄体型
		SELECTCASE RAND:3
			CASE 0
				PRINT 幼い割れ目で
			CASE 1
				PRINT 未成熟な秘裂で
			CASE 2
				PRINT まだ毛も生えていない膣口で
		ENDSELECT
	ELSE
		SELECTCASE RAND:3
			CASE 0
				PRINT 秘裂で
			CASE 1
				PRINT 先端を膣口にあてがい
			CASE 2
				PRINT 柔らかな秘肉で
		ENDSELECT
	ENDIF
	PRINTL 咥え込んだ……
ELSE
	IF OVER_GENITAL(TARGET) != ""
		PRINTFORM %CALLNAME:TARGET%の
		IF CFLAG:挿入可
			SIF TEQUIP:下半身下着
				PRINTFORM %CSTR:下半身下着%を降ろし、
		ELSE
			SIF TEQUIP:下半身上着 || TEQUIP:全身上着
				PRINTFORM OVER_GENITAL(TARGET)
			SIF TEQUIP:下半身下着 && (TEQUIP:下半身上着 || TEQUIP:全身上着)
				PRINT と
			SIF TEQUIP:下半身下着
				PRINTFORM %CSTR:下半身下着%
			PRINT を脱がして、
		ENDIF
		SIF !CFLAG:陥落
			PRINT 躊躇する
		SIF TALENT:小柄体型
			PRINT 小さな
		PRINT 身体を抱き寄せながら、
	ELSE
		PRINTFORM %CALLNAME:TARGET%の身体を抱き上げ、ゆっくりと降ろしながら、
	ENDIF
	;潤滑LV4以上
	SIF PALAM:潤滑 >= PALAMLV:4
		PRINT 濡れそぼつ
	;処女
	SIF TALENT:処女
		PRINT 男を知らない
	;ケモ
	IF TALENT:ファーリー
		SELECTCASE RAND:3
			CASE 0
				PRINT 毛むくじゃらの割れ目を
			CASE 1
				PRINT 秘部の体毛を先端で掻き分け
			CASE 2
				PRINT 体毛に覆われる女性器をこじ開け
		ENDSELECT
	;無機物
	ELSEIF TALENT:無機物
		SELECTCASE RAND:2
			CASE 0
				PRINT 無機質な膣口を指で拡げて
			CASE 1
				PRINT 形だけの生殖器に
		ENDSELECT
	;小柄体型
	ELSEIF TALENT:小柄体型
		SELECTCASE RAND:3
			CASE 0
				PRINT 幼い割れ目に
			CASE 1
				PRINT 未成熟な秘裂に
			CASE 2
				PRINT まだ毛も生えていない膣口に
		ENDSELECT
	ELSE
		SELECTCASE RAND:3
			CASE 0
				PRINT 秘裂に
			CASE 1
				PRINT 膣口に先端をあてがい
			CASE 2
				PRINT 柔らかな秘肉をこじ開け
		ENDSELECT
	ENDIF
	PRINTL 挿入した……
ENDIF

IF TALENT:アニマル
	SIF CFLAG:依存度 >= 1500
		PRINTFORML %CALLNAME:TARGET%は前足を%CALLNAME:PLAYER%の肩にかけ、顔を舐めてきた……
;陥落済み 縄無し
ELSEIF CFLAG:陥落 && !TEQUIP:縄
	PRINTFORM %CALLNAME:TARGET%は
	SIF (TEQUIP:下半身上着 || TEQUIP:全身上着) && CFLAG:挿入可
			PRINTFORM %OVER_GENITAL(TARGET)%に隠れる結合部を締め付け、
	PRINTFORM %CALLNAME:PLAYER%に
	;ボールギャグの有無
	SIF !TEQUIP:ボールギャグ
		PRINT 口付けをしつつ
	SELECTCASE TALENT:バストサイズ
		;絶壁or男
		CASE 0, 1
			PRINT 乳首を擦り付けながら
		;貧乳
		CASE 2
			PRINT 薄い胸を押し付けながら
		;普通
		CASE 3
			PRINT ぐっと体を寄せ付けながら
		;巨乳か爆乳
		CASE 4, 5
			PRINT 豊満な胸を擦り付けながら
	ENDSELECT
	PRINTL 首に手を回して抱きついてきた……
ENDIF
WAIT

;-------------------------------------------------
;背面座位
;-------------------------------------------------
@MESSAGE_COM_背面座位
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	SELECTCASE PREVCOMS
		CASE "背面座位"
			PRINTFORMW %CALLNAME:TARGET%を背後から抱きしめ、再び%CALLNAME:TARGET%の身体を揺らし始めた……
			RETURN 0
		CASE "後背位", "騎乗位"
			PRINTFORMW 膣内からペニスを抜き去ることなく%CALLNAME:TARGET%の身体を背中から抱き寄せ、身体を揺さぶり始めた……
			RETURN 0
		CASE "正常位", "対面座位"
			PRINTFORM 挿入したまま反対を向かせた%CALLNAME:TARGET%の身体を
			;前回の調教が正常位
			IF PREVCOMS == "正常位"
				PRINT 抱き上げ、
			ELSE
				PRINT ぎゅっと抱き寄せ、
			ENDIF
			PRINTW 激しく腰を揺さぶり始めた……
			RETURN 0
	ENDSELECT
ENDIF

SIF 上級売春中
	PRINTFORML %CALLNAME:TARGET%は後ろから抱きしめられて犯されることをご所望だ。膝立ちで背中を向けている
IF OVER_GENITAL(TARGET) != ""
	PRINTFORM %CALLNAME:TARGET%の
	IF CFLAG:挿入可
		SIF TEQUIP:下半身下着
			PRINTFORM %CSTR:下半身下着%を降ろし、
	ELSE
		SIF TEQUIP:下半身上着 || TEQUIP:全身上着
			PRINTFORM %OVER_GENITAL(TARGET)%
		SIF TEQUIP:下半身下着 && (TEQUIP:下半身上着 || TEQUIP:全身上着)
			PRINT と
		SIF TEQUIP:下半身下着
			PRINTFORM %CSTR:下半身下着%
		PRINT を脱がして、
	ENDIF
	SIF !CFLAG:陥落
		PRINT 躊躇する
	SIF TALENT:小柄体型
		PRINT 小さな
	PRINT 背中を抱き寄せながら、
ELSE
	PRINTFORM %CALLNAME:TARGET%の身体を後ろから抱き上げ、
ENDIF

;潤滑LV4以上
SIF PALAM:潤滑 >= PALAMLV:4
	PRINT 濡れそぼつ
;処女
SIF TALENT:処女
	PRINT 男を知らない
;ケモ
IF TALENT:ファーリー
	SELECTCASE RAND:3
		CASE 0
			PRINT 毛むくじゃらの割れ目を
		CASE 1
			PRINT 秘部の体毛を先端で掻き分け
		CASE 2
			PRINT 体毛に覆われる女性器をこじ開け
	ENDSELECT
;無機物
ELSEIF TALENT:無機物
	SELECTCASE RAND:2
		CASE 0
			PRINT 無機質な膣口を指で拡げて
		CASE 1
			PRINT 形だけの生殖器を
	ENDSELECT
;小柄体型
ELSEIF TALENT:小柄体型
	SELECTCASE RAND:3
		CASE 0
			PRINT 幼い割れ目を
		CASE 1
			PRINT 未成熟な秘裂を
		CASE 2
			PRINT まだ毛も生えていない膣口を
	ENDSELECT
ELSE
	SELECTCASE RAND:2
		CASE 0
			PRINT 秘裂を
		CASE 1
			PRINT 柔らかな秘肉を
	ENDSELECT
ENDIF
SELECTCASE RAND:2
	CASE 0
		PRINTL 深く突き上げた……
	CASE 1
		PRINTL ゆっくりとそそり勃つ剛直に落としていった……
ENDSELECT

;陥落済み
IF CFLAG:陥落
	PRINTFORM 後ろから
	SELECTCASE TALENT:バストサイズ
		;絶壁or男
		CASE 0, 1
			PRINT 乳首を弄り回すと、
		;貧乳
		CASE 2
			PRINT 薄い胸を優しく撫で回すと、
		;普通
		CASE 3
			PRINT 乳房を手で愛撫すると、
		;巨乳か爆乳
		CASE 4, 5
			PRINT 豊満な胸を揉みしだくと、
	ENDSELECT
	PRINTFORM %CALLNAME:TARGET%は
	IF (TEQUIP:下半身上着 || TEQUIP:全身上着) && CFLAG:挿入可
		PRINTFORM %OVER_GENITAL(TARGET)%に隠れる結合部を締め付けて
	ELSE
		PRINT 背を仰け反らせて
	ENDIF
	PRINTFORML %CALLNAME:PLAYER%に身を委ねた……
ENDIF
WAIT

;-------------------------------------------------
;下着ずらし挿入
;-------------------------------------------------
@MESSAGE_COM_下着ずらし挿入
PRINTFORM %CALLNAME:PLAYER%は\@ TEQUIP:ゲームセンター ? 壁に手をついた #  \@%CALLNAME:TARGET%の%CSTR:下半身下着%をずらして、その露になった
;処女
IF TALENT:処女
	PRINT 男を知らない
;潤滑LV4以上
ELSEIF PALAM:潤滑 >= PALAMLV:4
	PRINT 愛液の滴る
ENDIF
;ケモ
IF TALENT:ファーリー
	SELECTCASE RAND:3
		CASE 0
			PRINT 毛むくじゃらの割れ目を
		CASE 1
			PRINT 秘部の体毛を先端で掻き分け
		CASE 2
			PRINT 体毛に覆われる女性器をこじ開け
	ENDSELECT
;無機物
ELSEIF TALENT:無機物
	SELECTCASE RAND:2
		CASE 0
			PRINT 無機質な膣口を指で拡げて
		CASE 1
			PRINT 形だけの生殖器を
	ENDSELECT
;小柄体型
ELSEIF TALENT:小柄体型
	SELECTCASE RAND:3
		CASE 0
			PRINT 幼い割れ目を
		CASE 1
			PRINT 未成熟な秘裂を
		CASE 2
			PRINT まだ毛も生えていない膣口を押し広げ
	ENDSELECT
ELSE
	SELECTCASE RAND:3
		CASE 0
			PRINT 秘裂を
		CASE 1
			PRINT 膣口に先端をあてがい
		CASE 2
			PRINT 柔らかな秘肉をこじ開け
	ENDSELECT
ENDIF
;陥落済み
IF CFLAG:陥落 && !TEQUIP:ゲームセンター
	PRINTL 貫いた
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%に足を絡め腰を押し付けてきた……
ELSE
	PRINTL 貫いた……
ENDIF
WAIT

;-------------------------------------------------
;正常位アナル
;-------------------------------------------------
@MESSAGE_COM_正常位アナル
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	;前回コマンド
	SELECTCASE PREVCOMS
		CASE "正常位アナル"
			;調教者がペニス持ち
			IF PENIS(PLAYER)
				PRINTFORMW %CALLNAME:TARGET%の菊穴の感触を味わいながら、再び腰を動かし始めた……
			;ペニスバンドを使用している場合
			ELSE
				PRINTFORMW %CALLNAME:TARGET%の腸内から抜かずに、再び腰を動かし始めた……
			ENDIF
			RETURN 0
		CASE "後背位アナル", "対面座位アナル", "背面座位アナル", "騎乗位アナル"
			PRINTFORMW 腸内から抜き去ることなく%CALLNAME:TARGET%を仰向けに寝かせ、再び腰を動かし始めた……
			RETURN 0
	ENDSELECT
ENDIF

SIF 上級売春中 && (ABL:ゲイ中毒 || SEX(TARGET) == 2)
	PRINTFORML %CALLNAME:TARGET%はおもむろに仰向けになっては、後ろの穴を犯すよう求めてくる
PRINTFORM %CALLNAME:TARGET%の
IF TEQUIP:下半身上着 || (TEQUIP:全身上着 && CFLAG:着衣併用 != 1)
	IF CFLAG:挿入可
		PRINTFORM %OVER_GENITAL(TARGET)%の中に手を忍ばせ\@ TEQUIP:下半身下着 ? %CSTR:下半身下着%をずらして、# 裾を捲り上げ、 \@
	ELSE
		PRINTFORM %OVER_GENITAL(TARGET)%\@ TEQUIP:下半身下着 ? と%CSTR:下半身下着% #  \@を脱がして、その
	ENDIF
ELSEIF TEQUIP:下半身下着
	PRINTFORM %CSTR:下半身下着%をずらし、無防備な
;潤滑LV4以上
ELSEIF PALAM:潤滑 >= PALAMLV:4
	PRINT 粘液にまみれる
ENDIF
;Ａ感覚LV4以上
SIF ABL:Ａ感覚 >= 4
	PRINT すっかり開発されきった
;陥落済み
IF TALENT:アニマル
	PRINTL アナルに、後ろ足を掴みながら突き入れた……
	SIF CFLAG:依存度 >= 1500
		PRINTFORML %CALLNAME:TARGET%は嬉しそうな鳴き声を上げ、舌を出して飼い主の%CALLNAME:PLAYER%に媚びている……
ELSEIF CFLAG:陥落
	PRINTL 尻穴にペニスを突き入れた
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%に足を絡め腰を押し付けてきた……
ELSE
	PRINTL アナルを貫いた……
ENDIF
SIF 上級売春中 && !ABL:ゲイ中毒 && SEX(TARGET) == 1
	PRINTFORML %CALLNAME:TARGET%は自分が犯されることなど想定外だったのか、戸惑って暴れながらも痛みと、僅かな快感に屈して力が抜けていく……
WAIT

;-------------------------------------------------
;後背位アナル
;-------------------------------------------------
@MESSAGE_COM_後背位アナル
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	;前回コマンド
	SELECTCASE PREVCOMS
		CASE "後背位アナル"
			;調教者がペニス持ち
			IF PENIS(PLAYER)
				PRINTFORMW %CALLNAME:TARGET%の尻穴の感触を味わいながら、再び腰を動かし始めた……
			;ペニスバンドを使用している場合
			ELSE
				PRINTFORMW %CALLNAME:TARGET%の腸内から抜かずに、再び腰を動かし始めた……
			ENDIF
			RETURN 0
		CASE "正常位アナル"
			PRINTFORMW 腸内から抜き去ることなく%CALLNAME:TARGET%の後ろに回り、再び腰を動かし始めた……
			RETURN 0
		CASE "対面座位アナル", "背面座位アナル", "騎乗位アナル"
			PRINTFORMW 挿入したまま%CALLNAME:TARGET%を寝かせて四つん這いにし、再び腰を動かし始めた……
			RETURN 0
	ENDSELECT
ENDIF

IF TEQUIP:ゲームセンター
	PRINTFORM %CALLNAME:TARGET%を前かがみにさせ、
ELSE
	PRINTFORM %CALLNAME:TARGET%を四つん這いにさせ、
ENDIF
IF TEQUIP:下半身上着 || (TEQUIP:全身上着 && CFLAG:着衣併用 != 1)
	IF CFLAG:挿入可
		PRINTFORM %OVER_GENITAL(TARGET)%に隠れた\@ TEQUIP:下半身下着 ? %CSTR:下半身下着%をずらして、# 秘部を暴き、 \@
	ELSE
		PRINTFORM %OVER_GENITAL(TARGET)%\@ TEQUIP:下半身下着 ? と%CSTR:下半身下着% #  \@を脱がして、露わになった
	ENDIF
ELSEIF TEQUIP:下半身下着
	PRINTFORM %CSTR:下半身下着%をずらして、無防備な
;潤滑LV4以上
ELSEIF PALAM:潤滑 >= PALAMLV:4
	PRINT 粘液にまみれた
ENDIF
;Ａ感覚LV4以上
SIF ABL:Ａ感覚 >= 4
	PRINT すっかり開発されきった
IF (CFLAG:陥落 && !TEQUIP:ゲームセンター) || (TALENT:アニマル && CFLAG:依存度 >= 1500)
	PRINTL 尻穴をゆっくりと貫いた……
	IF TALENT:尻尾
		PRINTFORML %CALLNAME:TARGET%は尻尾を振りながら尻穴をきつく締め付けている……
	ELSE
		PRINTFORML %CALLNAME:TARGET%は尻を突き上げてさらに深くまで%CALLNAME:PLAYER%を誘い込んでくる……
	ENDIF
ELSE
	PRINTL アヌスの最奥まで突き入れた……
ENDIF
IF 上級売春中
	IF TALENT:マゾ
		PRINTFORM 乱暴に犯されて喜ぶ%CALLNAME:TARGET%をさらに激しく蹂躙すると、
		IF PENIS(TARGET)
			PRINTL 揺れるペニスから先走りを振りまいている……
		ELSE
			PRINTL 膣から溢れる愛液が太腿を伝っている……
		ENDIF
	ELSE
		SELECTCASE SEX(TARGET)
			CASE 1
				IF ABL:ゲイ中毒
					PRINTFORML %CALLNAME:TARGET%は奥を一突きするたびにびくんと体を跳ねさせ、掘られる快感に悶えている……
				ELSE
					IF ABL:Ａ感覚
						PRINTFORML 以外にもアナルの経験があるらしい%CALLNAME:TARGET%は、掘られるや否や悦びの嬌声を上げている……
					ELSE
						PRINTFORML 女を買いに来たにも関わらず犯されてしまった%CALLNAME:TARGET%は、男としてのプライドを砕かれて涙を浮かべている……
					ENDIF
				ENDIF
			CASE 2
				IF ABL:Ａ感覚
					PRINTFORML 尻穴ですんなりと受け入れた%CALLNAME:TARGET%は淫らな息遣いで%CALLNAME:PLAYER%の腰使いを加速させている……
				ELSE
					PRINTFORML いざ後ろを使ってはみるものの、%CALLNAME:TARGET%は痛みと苦しさ息も絶え絶えだ……
				ENDIF
		ENDSELECT
	ENDIF
ENDIF
IF TEQUIP:ゲームセンター
	PRINTFORM %CALLNAME:TARGET%は壁に手をついて
	IF ABL:Ａ感覚 >= 5
		SELECTCASE SEX(TARGET)
			CASE 1
				PRINTL 声を押し殺そうとするが、前立腺での強烈な快楽に喘ぎを抑えきれずにいる……
			CASE 2
				PRINTL 快感に悶え、膣から溢れる愛液を太腿に滴らせている……
		ENDSELECT
	ELSE
		PRINTL 必死に声を押し殺している……
	ENDIF
ENDIF
WAIT
;-------------------------------------------------
;騎乗位アナル
;-------------------------------------------------
@MESSAGE_COM_騎乗位アナル
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	;前回コマンド
	SELECTCASE PREVCOMS
		CASE "騎乗位アナル"
			PRINTFORMW %CALLNAME:TARGET%は挿入されたままのモノを締め付け、再び腰を動かし始めた……
			RETURN 0
		CASE "正常位アナル", "後背位アナル"
			PRINTFORMW 腸内から抜き去ることなく%CALLNAME:TARGET%を抱き上げ、下から腰を突き上げた……
			RETURN 0
		CASE "対面座位アナル", "背面座位アナル"
			PRINTFORMW %CALLNAME:MASTER%は挿入したまま横になり、下から腰を突き上げた……
			RETURN 0
	ENDSELECT
ENDIF

PRINTFORM %CALLNAME:TARGET%は
IF OVER_GENITAL(TARGET) != ""
	IF CFLAG:挿入可
		SIF TEQUIP:下半身下着
			PRINTFORM %CSTR:下半身下着%を降ろし、
		SIF TEQUIP:下半身上着 || TEQUIP:全身上着
			PRINTFORM %OVER_GENITAL(TARGET)%をたくし上げ
	ELSE
		SIF TEQUIP:下半身上着 || TEQUIP:全身上着
			PRINTFORM %OVER_GENITAL(TARGET)%
		SIF TEQUIP:下半身下着 && (TEQUIP:下半身上着 || TEQUIP:全身上着)
			PRINT と
		SIF TEQUIP:下半身下着
			PRINTFORM %CSTR:下半身下着%
		PRINT を脱いで
	ENDIF
ENDIF
PRINTFORM %CALLNAME:PLAYER%の上にまたがると、
;Ａ感覚LV4以上
SIF ABL:Ａ感覚 >= 4
	PRINT すっかり開発されきった
;潤滑LV4以上
SIF PALAM:潤滑 >= PALAMLV:4
	PRINT 粘液にまみれる
PRINTL 尻穴でゆっくりと勃起するペニスを飲み込んでいった……
;陥落済み
IF CFLAG:陥落
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%のペニス深く受け入れ、
	;小柄体型
	SIF TALENT:小柄体型
		PRINT 小さな身体で懸命に
	SELECTCASE TALENT:バストサイズ
		;巨乳
		CASE 4
			PRINT 大きく胸を揺らしながら
		;爆乳
		CASE 5
			PRINT 跳ねる胸に身体ごと揺らされながら
	ENDSELECT
	PRINTL 自ら腰を動かしている……
ENDIF
WAIT

;-------------------------------------------------
;対面座位アナル
;-------------------------------------------------
@MESSAGE_COM_対面座位アナル
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	SELECTCASE PREVCOMS
		CASE "対面座位アナル"
			PRINTFORMW %CALLNAME:PLAYER%は%CALLNAME:TARGET%の体を離さずに、腰を動かし続けた……
			RETURN 0
		CASE "正常位アナル", "騎乗位アナル"
			PRINTFORMW 腸内からペニスを抜き去ることなく%CALLNAME:TARGET%の身体をぎゅっと抱き寄せ、腰を動かし続けた……
			RETURN 0
		CASE "後背位アナル", "背面座位アナル"
			PRINTFORM 挿入したまま身体をこちらに向かせ
			;前回の調教が後背位アナル
			IF PREVCOMS == "後背位アナル"
				PRINT て抱き上げ、
			ELSE
				PRINT るとぎゅっと抱き寄せ、
			ENDIF
			PRINTW 腰を動かし続けた……
			RETURN 0
	ENDSELECT
ENDIF

IF OVER_GENITAL(TARGET) != ""
	PRINTFORM %CALLNAME:TARGET%の
	IF CFLAG:挿入可
		SIF TEQUIP:下半身下着
			PRINTFORM %CSTR:下半身下着%を降ろし、
	ELSE
		SIF TEQUIP:下半身上着 || TEQUIP:全身上着
			PRINTFORM %OVER_GENITAL(TARGET)%
		SIF TEQUIP:下半身下着 && (TEQUIP:下半身上着 || TEQUIP:全身上着)
			PRINT と
		SIF TEQUIP:下半身下着
			PRINTFORM %CSTR:下半身下着%
		PRINT を脱がして、
	ENDIF
	SIF !CFLAG:陥落
		PRINT 躊躇する
	SIF TALENT:小柄体型
		PRINT 小さな
	PRINT 身体を抱き寄せながら、
ELSE
	PRINTFORM %CALLNAME:TARGET%の身体を抱き上げ、ゆっくりと降ろしながら、
ENDIF

;潤滑LV4以上
sIF PALAM:潤滑 >= PALAMLV:4
	PRINT 粘液にまみれる
;Ａ感覚LV4以上
SIF ABL:Ａ感覚 >= 4
	PRINT すっかり開発されきった
PRINTL アナルへと挿入した……

IF TALENT:アニマル
	SIF CFLAG:依存度 >= 1500
		PRINTFORML %CALLNAME:TARGET%は前足を%CALLNAME:PLAYER%の肩にかけ、顔を舐めてきた……
;陥落済み 縄無し
ELSEIF CFLAG:陥落 && !TEQUIP:縄
	PRINTFORM %CALLNAME:TARGET%は
	SIF (TEQUIP:下半身上着 || TEQUIP:全身上着) && CFLAG:挿入可
		PRINTFORM %OVER_GENITAL(TARGET)%に隠れる結合部を締め付け、
	PRINTFORM %CALLNAME:PLAYER%に
	;ボールギャグの有無
	SIF !TEQUIP:ボールギャグ
		PRINT 口付けをしつつ
	SELECTCASE TALENT:バストサイズ
		;絶壁or男
		CASE 0, 1
			PRINT 乳首を擦り付けながら
		;貧乳
		CASE 2
			PRINT 薄い胸を押し付けながら
		;普通
		CASE 3
			PRINT ぐっと体を寄せ付けながら
		;巨乳か爆乳
		CASE 4, 5
			PRINT 豊満な胸を擦り付けながら
	ENDSELECT
	PRINTL 首に手を回して抱きついてきた……
ENDIF
WAIT

;-------------------------------------------------
;背面座位アナル
;-------------------------------------------------
@MESSAGE_COM_背面座位アナル
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	SELECTCASE PREVCOMS
		CASE "背面座位アナル"
			PRINTFORMW %CALLNAME:TARGET%を背後から抱きしめ、再び%CALLNAME:TARGET%の身体を揺らし始めた……
			RETURN 0
		CASE "後背位アナル", "騎乗位アナル"
			PRINTFORMW 腸内からペニスを抜き去ることなく%CALLNAME:TARGET%の身体を背中から抱き寄せ、身体を揺さぶり始めた……
			RETURN 0
		CASE "正常位アナル", "対面座位アナル"
			PRINTFORM 挿入したまま反対を向かせた%CALLNAME:TARGET%の身体を
			;前回の調教が正常位アナル
			IF PREVCOMS == "正常位アナル"
				PRINT 抱き上げ、
			ELSE
				PRINT ぎゅっと抱き寄せ、
			ENDIF
			PRINTW 激しく腰を揺さぶり始めた……
			RETURN 0
	ENDSELECT
ENDIF

IF OVER_GENITAL(TARGET) != ""
	PRINTFORM %CALLNAME:TARGET%の
	IF CFLAG:挿入可
		SIF TEQUIP:下半身下着
			PRINTFORM %CSTR:下半身下着%を降ろし、
	ELSE
		SIF TEQUIP:下半身上着 || TEQUIP:全身上着
			PRINTFORM %OVER_GENITAL(TARGET)%
		SIF TEQUIP:下半身下着 && (TEQUIP:下半身上着 || TEQUIP:全身上着)
			PRINT と
		SIF TEQUIP:下半身下着
			PRINTFORM %CSTR:下半身下着%
		PRINT を脱がして、
	ENDIF
	SIF !CFLAG:陥落
		PRINT 躊躇する
	SIF TALENT:小柄体型
		PRINT 小さな
	PRINT 背中を抱き寄せながら、
ELSE
	PRINTFORM %CALLNAME:TARGET%の身体を後ろから抱き上げ、
ENDIF

;潤滑LV4以上
SIF PALAM:潤滑 >= PALAMLV:4
	PRINT 粘液にまみれる
;Ａ感覚LV4以上
SIF ABL:Ａ感覚 >= 4
	PRINT すっかり開発されきった
SELECTCASE RAND:2
	CASE 0
		PRINTL アナルを深く突き上げた……
	CASE 1
		PRINTL 尻穴をゆっくりとそそり勃つ剛直に落としていった……
ENDSELECT

;陥落済み
IF CFLAG:陥落
	PRINTFORM 後ろから
	SELECTCASE TALENT:バストサイズ
		;絶壁or男
		CASE 0, 1
			PRINT 乳首を弄り回すと、
		;貧乳
		CASE 2
			PRINT 薄い胸を優しく撫で回すと、
		;普通
		CASE 3
			PRINT 後ろから乳房を手で愛撫すると、
		;巨乳か爆乳
		CASE 4, 5
			PRINT 豊満な胸を揉みしだくと、
	ENDSELECT
	PRINTFORM %CALLNAME:TARGET%は
	IF (TEQUIP:下半身上着 || TEQUIP:全身上着) && CFLAG:挿入可
		PRINTFORM %OVER_GENITAL(TARGET)%に隠れる結合部を締め付けて
	ELSE
		PRINT 背を仰け反らせて
	ENDIF
	PRINTFORML %CALLNAME:PLAYER%に身体を委ねていた……
ENDIF
WAIT

;-------------------------------------------------
;下着ずらし挿入アナル
;-------------------------------------------------
@MESSAGE_COM_下着ずらし挿入アナル
PRINTFORM %CALLNAME:PLAYER%は\@ TEQUIP:ゲームセンター ? 壁に手をついた #  \@%CALLNAME:TARGET%の%CSTR:下半身下着%をずらして、その露になった
;潤滑LV4以上
SIF PALAM:潤滑 >= PALAMLV:4
	PRINT 粘液にまみれる
;Ａ感覚LV4以上
SIF ABL:Ａ感覚 >= 4
	PRINT すっかり開発されきった
;陥落済み
IF CFLAG:陥落 && !TEQUIP:ゲームセンター
	PRINTL アヌスを貫いた
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%に足を絡め腰を押し付けてきた……
ELSE
	PRINTL アヌスを貫いた……
ENDIF
WAIT

;-------------------------------------------------
;正常位させる
;-------------------------------------------------
@MESSAGE_COM_正常位させる
PRINTFORM %CALLNAME:PLAYER%は
IF TFLAG:前コマンド実行者 == PLAYER
	SELECTCASE PREVCOMS
		CASE "正常位させる"
			IF 上級売春中
				PRINTFORML %CALLNAME:TARGET%を喜ばせるために喘いでよがり、両手両足で%CALLNAME:TARGET%に抱きついた……
			ELSE
				PRINTFORML セックスの快感に子宮を疼かせながら膣内を締め付け、さらに激しく自分を抱くように命じた……
			ENDIF
		CASE "後背位させる"
			IF 上級売春中
				PRINTFORML %CALLNAME:TARGET%に言われるままに仰向けになり、行為を続けた……
			ELSE
				PRINTFORML 後ろからの交尾に夢中になる%CALLNAME:TARGET%を制止して仰向けになり、性交を続けた……
			ENDIF
		CASE "正常位アナルさせる"
			IF 上級売春中
				PRINTFORML 前の方に挿れたがる%CALLNAME:TARGET%のペニスを丁寧に拭いて、膣内に導いた……
			ELSE
				PRINTFORML 尻穴に何度も突き入れる%CALLNAME:TARGET%を制止して、今度は膣内へと導いた……
			ENDIF
		CASE "後背位アナルさせる"
			IF 上級売春中
				PRINTFORML アナルセックスを堪能した%CALLNAME:TARGET%のペニスを丁寧に拭いて、仰向けで膣での性交を始めた……
			ELSE
				PRINTFORML 後ろからのセックスに夢中になる%CALLNAME:TARGET%を制止して仰向けになり、次は愛液に濡れる膣口へと誘いこんだ……
			ENDIF
		CASEELSE
			GOTO ELSE
	ENDSELECT
ELSE
	$ELSE
	IF OVER_GENITAL(PLAYER) != ""
		IF CFLAG:PLAYER:挿入可
			SIF TEQUIP:PLAYER:下半身下着
				PRINTFORM %CSTR:PLAYER:下半身下着%を降ろし
			SIF TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着
				PRINTFORM %OVER_GENITAL(PLAYER)%をたくし上げ
			PRINT て
		ELSE
			SIF TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着
				PRINTFORM %OVER_GENITAL(PLAYER)%
			SIF TEQUIP:PLAYER:下半身下着 && (TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着)
				PRINT と
			SIF TEQUIP:PLAYER:下半身下着
				PRINTFORM %CSTR:PLAYER:下半身下着%
			PRINT を脱いで
		ENDIF
	ENDIF
	PRINTFORM 寝転がり、%CALLNAME:TARGET%の
	IF TALENT:アニマル
		PRINT ペニスの根本を掴み、
	ELSEIF !PENIS(TARGET)
		PRINT 股間から伸びるペニスバンドを
	ELSE
		SIF TALENT:童貞
			PRINT 女を知らない初心な
		PRINT ペニスを
	ENDIF
	PRINTL 自らの膣内に導いた……
ENDIF
IF 上級売春中
	IF TALENT:PLAYER:処女
		PRINTFORML 破瓜の血を流す%CALLNAME:PLAYER%に、%CALLNAME:TARGET%は驚きを見せている……
	ELSEIF TALENT:童貞
		PRINTFORML 初めての性交で緊張に息を荒げる%CALLNAME:TARGET%はうまく挿入できない。%CALLNAME:PLAYER%は互いの性器に手を添えながら優しく中へと導いた……
	ELSE
		IF TALENT:小柄体型 && !TALENT:PLAYER:小柄体型
			PRINTFORML 小さな体躯でしがみつきながら腰を振る%CALLNAME:TARGET%を、%CALLNAME:PLAYER%は胸に埋めるように強く抱きしめる……
		ELSEIF !TALENT:小柄体型 && TALENT:PLAYER:小柄体型
			PRINTFORML 細い足のつま先をぴんと伸ばす%CALLNAME:PLAYER%は、熱く太くて大きい剛直を、幼い膣でさらにきゅぅっと締め付けた……
		ELSE
			PRINTFORML %CALLNAME:TARGET%もまた息を乱し、快感に浸る表情で腰を動かしている……
		ENDIF
	ENDIF
ELSE
	IF TALENT:PLAYER:処女
		IF TALENT:アニマル
			PRINTFORML %CALLNAME:TARGET%は膣内から漏れ出す破瓜血を気にも留めず、交尾を続けている……
		ELSE
			IF FALLTYPE(TARGET) == 1 && PLAYER == MASTER
				PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の小さな悲鳴と共に膣内から漏れ出す破瓜の血に驚いたが、%CALLNAME:PLAYER%の初めての相手になれたことがこの上なく幸せのようだ……
			ELSE
				PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の小さな悲鳴と共に膣内から漏れ出す破瓜の血に驚き、複雑な表情をしている……
			ENDIF
		ENDIF
	ELSEIF TALENT:アニマル
		IF CFLAG:依存度 >= 1500
			PRINTFORML %CALLNAME:TARGET%は生温かい舌で%CALLNAME:PLAYER%の顔や体を舐めながら、交尾に夢中になっている……
		ELSE
			PRINTFORML %CALLNAME:TARGET%は本能のままに腰を振り続け、息を荒げている……
		ENDIF
	ELSEIF TALENT:童貞
		PRINTFORML 初めてで勝手の分からない%CALLNAME:TARGET%を、%CALLNAME:PLAYER%は腰を抱き寄せて一番奥まで誘い込んだ……
	ELSEIF CFLAG:陥落
		PRINTFORM %CALLNAME:TARGET%は\@ FALLTYPE(TARGET) == 1 ? 愛する #  \@%CALLNAME:PLAYER%とのセックスに、
		IF TALENT:小柄体型 && TALENT:PLAYER:小柄体型
			PRINT お互い子供のようにがむしゃらに抱き合いながら
		ELSEIF TALENT:小柄体型 && !TALENT:PLAYER:小柄体型
			PRINTFORM 小さな体躯で必死に%CALLNAME:PLAYER%の体にしがみついて
		ELSEIF !TALENT:小柄体型 && TALENT:PLAYER:小柄体型
			PRINT ペニスを咥え込む幼い体を気遣いながら優しく
		ELSE
			PRINT 夢中で
		ENDIF
		PRINTL 腰を振っている……
	ENDIF
ENDIF
WAIT

;-------------------------------------------------
;後背位させる
;-------------------------------------------------
@MESSAGE_COM_後背位させる
PRINTFORM %CALLNAME:PLAYER%は
IF TFLAG:前コマンド実行者 == PLAYER
	SELECTCASE PREVCOMS
		CASE "正常位させる"
			PRINTFORML 膣内に挿入して腰を振る%CALLNAME:TARGET%を制して四つん這いになり、膣口を拡げてまた最奥へと導いた……
		CASE "後背位させる"
			PRINTFORML 後ろから何度も膣内を突かれ、音を立てて愛液を零しながら更なる快感を要求した……
		CASE "正常位アナルさせる"
			PRINTFORML 尻穴に何度も突き入れる%CALLNAME:TARGET%を制して四つん這いになり、今度は膣内へと導いた……
		CASE "後背位アナルさせる"
			PRINTFORML 後ろからアナルへの挿入を繰り返す%CALLNAME:TARGET%に声をかけ、愛液の滴る膣口への挿入を促した……
		CASEELSE
			GOTO ELSE
	ENDSELECT
ELSE
	$ELSE
	IF OVER_GENITAL(PLAYER) != ""
		IF CFLAG:PLAYER:挿入可
			SIF TEQUIP:PLAYER:下半身下着
				PRINTFORM %CSTR:PLAYER:下半身下着%を降ろして
		ELSE
			SIF TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着
				PRINTFORM %OVER_GENITAL(PLAYER)%
			SIF TEQUIP:PLAYER:下半身下着 && (TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着)
				PRINT と
			SIF TEQUIP:PLAYER:下半身下着
				PRINTFORM %CSTR:PLAYER:下半身下着%
			PRINT を脱いで
		ENDIF
	ENDIF
	IF TEQUIP:ゲームセンター
		PRINT 壁に手をつき、
	ELSE
		PRINT 四つん這いになると、
	ENDIF
	PRINTFORM %CALLNAME:TARGET%の
	IF TALENT:アニマル
		PRINT ペニスの根本を掴み、
	ELSEIF !PENIS(TARGET)
		PRINT 股間から伸びるペニスバンドを
	ELSE
		SIF TALENT:童貞
			PRINT 女を知らない初心な
		PRINT ペニスを
	ENDIF
	SIF (TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着) && CFLAG:PLAYER:挿入可
		PRINTFORM %OVER_GENITAL(PLAYER)%の中から覗く
	PRINTL 自らの膣内に導いた……
ENDIF

IF 上級売春中
	IF TALENT:PLAYER:処女
		PRINTFORML 破瓜の血を流す%CALLNAME:PLAYER%に、%CALLNAME:TARGET%は驚きを見せている……
	ELSEIF TALENT:童貞
		PRINTFORML 初めての性交で緊張に息を荒げる%CALLNAME:TARGET%はうまく挿入できない。%CALLNAME:PLAYER%は自らの秘所を両手で拡げて初めてのペニスを受け入れた……
	ELSE
		IF TALENT:小柄体型 && !TALENT:PLAYER:小柄体型
			PRINTFORML 膝をがくがくとさせて腰を振る%CALLNAME:TARGET%の体に合わせて%CALLNAME:PLAYER%は腰の突き上げたり引いたりを繰り返す……
		ELSEIF !TALENT:小柄体型 && TALENT:PLAYER:小柄体型
			PRINTFORML %CALLNAME:TARGET%に半ば持ち上げられるように犯される%CALLNAME:PLAYER%は、よく透る高い声で%CALLNAME:TARGET%を惑わせるように喘いだ……
		ELSE
			PRINTFORML %CALLNAME:TARGET%もまた息を乱し、快感に浸る表情で腰を振っている……
		ENDIF
	ENDIF
ELSE
	IF TALENT:アニマル
		IF CFLAG:依存度 >= 1500
			PRINTFORML %CALLNAME:TARGET%は生温かい舌で%CALLNAME:PLAYER%の首や背中を舐めながら、交尾に夢中になっている……
		ELSE
			PRINTFORML %CALLNAME:TARGET%は本能のままに腰を振り続け、息を荒げている……
		ENDIF
	ELSEIF TALENT:童貞
		PRINTFORML 初めてで勝手の分からない%CALLNAME:TARGET%を、%CALLNAME:PLAYER%は尻を振って締め付けた……
	ELSEIF CFLAG:陥落
		PRINTFORM %CALLNAME:TARGET%は\@ FALLTYPE(TARGET) == 1 ? 愛する #  \@%CALLNAME:PLAYER%とのセックスに、
		IF TALENT:小柄体型 && TALENT:PLAYER:小柄体型
			PRINT 互いの幼い下半身をぶつけ合いながら
		ELSEIF TALENT:小柄体型 && !TALENT:PLAYER:小柄体型
			PRINTFORM 小さな体で必死に%CALLNAME:PLAYER%の体にしがみついて
		ELSEIF !TALENT:小柄体型 && TALENT:PLAYER:小柄体型
			PRINT ペニスを咥え込む幼い体を気遣いながら優しく
		ELSE
			SELECTCASE TALENT:バストサイズ
				CASE 1, 2
					PRINT 薄い胸を擦り付けるように覆いかぶさり
				CASE 3, 4, 5
					PRINT 乳房を押し付けながら
			ENDSELECT
			PRINT 夢中で
		ENDIF
		PRINTL 腰を振っている……
	ENDIF
ENDIF
WAIT

;-------------------------------------------------
;逆レイプ
;-------------------------------------------------
@MESSAGE_COM_逆レイプ
;前回と今回の調教者が同じ
IF PLAYER == TFLAG:前コマンド実行者
	;前回コマンド
	SELECTCASE PREVCOMS
		CASE "逆レイプ"
			PRINTFORMW %CALLNAME:PLAYER%は%CALLNAME:TARGET%のペニスをきつく食い締めたまま、執拗に腰を振りたてた……
			RETURN 0
	ENDSELECT
ENDIF

;奴隷着衣中
IF OVER_GENITAL(TARGET) != ""
	IF PALAM:欲情 <= PALAMLV:3
		PRINTFORML %CALLNAME:TARGET%の%OVER_GENITAL(TARGET)%の上からペニスを触り、段々と勃起するそれを解放するように衣服をずらす
	ELSE
		PRINTFORML %OVER_GENITAL(TARGET)%の中で窮屈そうに膨らむ%CALLNAME:TARGET%の男根を露出させ、軽く擦って準備を整える
	ENDIF
ENDIF

PRINTFORM %CALLNAME:PLAYER%は
SIF PLAYER == ASSI && !CFLAG:ASSI:陥落
	PRINTFORM 横目で%CALLNAME:MASTER%の機嫌を伺いながら、命じられた通りに
SIF 上級売春中
	PRINTFORM 期待の視線を向ける%CALLNAME:TARGET%に見せつけるように
IF OVER_GENITAL(PLAYER) != ""
	IF CFLAG:PLAYER:挿入可
		SIF TEQUIP:PLAYER:下半身下着
			PRINTFORM %CSTR:PLAYER:下半身下着%を降ろし、
		SIF TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着
			PRINTFORM %OVER_GENITAL(PLAYER)%をたくし上げて
	ELSE
		SIF TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着
			PRINTFORM %OVER_GENITAL(PLAYER)%
		SIF TEQUIP:PLAYER:下半身下着 && (TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着)
			PRINT と
		SIF TEQUIP:PLAYER:下半身下着
			PRINTFORM %CSTR:PLAYER:下半身下着%
		PRINT を脱いで、
	ENDIF
ENDIF
IF 上級売春中
	PRINTFORM 硬くそそり勃つペニスの先端へ膣口を擦りつけ、ゆっくりと
ELSE
	PRINTFORM %CALLNAME:TARGET%のペニスを強引に
ENDIF
SELECTCASE TFLAG:奴隷挿入箇所
	;膣
	CASE 1
		;調教者が処女
		SIF TALENT:PLAYER:処女
			PRINT 男を受け入れた事のない
		;調教者が小柄体型
		IF TALENT:PLAYER:小柄体型
			PRINT 小さな女性器で
		ELSE
			PRINT ヴァギナの奥まで
		ENDIF
		PRINTL 咥えこんだ……
	;アナル
	CASE 2
		PRINTL 自らの尻穴で咥え込んだ……
ENDSELECT
WAIT

;-------------------------------------------------
;正常位アナルさせる
;-------------------------------------------------
@MESSAGE_COM_正常位アナルさせる
PRINTFORM %CALLNAME:PLAYER%は
IF TFLAG:前コマンド実行者 == PLAYER
	SELECTCASE PREVCOMS
		CASE "正常位させる"
			IF 上級売春中
				PRINTFORML アナルでやりたいと言い出した%CALLNAME:TARGET%に対し、%CALLNAME:PLAYER%はさらに足を曲げて尻穴を晒して応えた……
			ELSE
				PRINTFORML 膣内に何度も突き入れる%CALLNAME:TARGET%を制止して、今度はアナルでやらせることにした……
			ENDIF
		CASE "後背位させる"
			IF 上級売春中
				PRINTFORML 膣に飽き足らず情欲を募らせる%CALLNAME:TARGET%は、%CALLNAME:TARGET%を仰向けにして顔を合わせると今度はアナルを犯してきた……
			ELSE
				PRINTFORML 後ろからの交尾に夢中になる%CALLNAME:TARGET%を制止して仰向けになり、今度はアナルに挿れさせた……
			ENDIF
		CASE "正常位アナルさせる"
			IF 上級売春中
				PRINTFORML アナルセックスに夢中になる%CALLNAME:TARGET%に激しく抱かれ、%CALLNAME:PLAYER%はくぐもった喘ぎ声を抑えきれずにいる……
			ELSE
				PRINTFORML 尻穴を犯される快感に%CALLNAME:TARGET%を抱きしめてさらに激しくするように命じた……
			ENDIF
		CASE "後背位アナルさせる"
			IF 上級売春中
				PRINTFORML %CALLNAME:TARGET%はバックで犯される%CALLNAME:PLAYER%を仰向けにして、アナルセックスの続きを楽しんだ……
			ELSE
				PRINTFORML 後ろからのセックスに夢中になる%CALLNAME:TARGET%を制止して仰向けになり、アナルでの性交を続けた……
			ENDIF
		CASEELSE
			GOTO ELSE
	ENDSELECT
ELSE
	$ELSE
	IF OVER_GENITAL(PLAYER) != ""
		IF CFLAG:PLAYER:挿入可
			SIF TEQUIP:PLAYER:下半身下着
				PRINTFORM %CSTR:PLAYER:下半身下着%を降ろし
			SIF TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着
				PRINTFORM %OVER_GENITAL(PLAYER)%をたくし上げ
			PRINT て
		ELSE
			SIF TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着
				PRINTFORM %OVER_GENITAL(PLAYER)%
			SIF TEQUIP:PLAYER:下半身下着 && (TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着)
				PRINT と
			SIF TEQUIP:PLAYER:下半身下着
				PRINTFORM %CSTR:PLAYER:下半身下着%
			PRINT を脱いで
		ENDIF
	ENDIF
	PRINTFORM 寝転がり、%CALLNAME:TARGET%の
	IF TALENT:アニマル
		PRINT ペニスの根本を掴み、
	ELSEIF !PENIS(TARGET)
		PRINT 股間から伸びるペニスバンドを
	ELSE
		SIF TALENT:童貞
			PRINT 女を知らない初心な
		PRINT ペニスを
	ENDIF
	PRINTL 自らのアナルに導いた……
ENDIF

IF 上級売春中
IF TALENT:童貞
		PRINTFORML 童貞をアナルで捨てようとする物好きな%CALLNAME:TARGET%だが、きつい尻穴と緊張が重なり一苦労を要した……
	ELSE
		IF PENIS(PLAYER)
			SELECTCASE SEX(PLAYER)
				CASE 1
					PRINTFORML 反り返った剛直で容赦無く前立腺を抉ってくる%CALLNAME:TARGET%の腰使いで、%CALLNAME:PLAYER%は嗚咽のような嬌声を上げている……
				CASE 2
					PRINTFORML %CALLNAME:TARGET%は、尻穴を突かれるたびに揺れる%CALLNAME:PLAYER%のペニスを愉快げに見つめている……
			ENDSELECT
		ELSE
			PRINTFORML %CALLNAME:TARGET%は、結合部がしっかり見えるように%CALLNAME:PLAYER%の腰を持ち上げて激しく犯してくる……
		ENDIF
	ENDIF
ELSE
	IF TALENT:アニマル
		IF CFLAG:依存度 >= 1500
			PRINTFORML %CALLNAME:TARGET%は生温かい舌で%CALLNAME:PLAYER%の顔や体を舐めながら、交尾に夢中になっている……
		ELSE
			PRINTFORML %CALLNAME:TARGET%は本能のままに腰を振り続け、息を荒げている……
		ENDIF
	ELSEIF TALENT:童貞
		PRINTFORML 初めてで勝手の分からない%CALLNAME:TARGET%を、%CALLNAME:PLAYER%は腰を抱き寄せて一番奥まで誘い込んだ……
	ELSEIF CFLAG:陥落
		PRINTFORM %CALLNAME:TARGET%は\@ FALLTYPE(TARGET) == 1 ? 愛する #  \@%CALLNAME:PLAYER%とのセックスに、
		IF TALENT:小柄体型 && TALENT:PLAYER:小柄体型
			PRINT お互い子供のようにがむしゃらに抱き合いながら
		ELSEIF TALENT:小柄体型 && !TALENT:PLAYER:小柄体型
			PRINTFORM 小さな体で必死に%CALLNAME:PLAYER%の体にしがみついて
		ELSEIF !TALENT:小柄体型 && TALENT:PLAYER:小柄体型
			PRINT ペニスを咥え込む幼い体を気遣いながら優しく
		ELSE
			PRINT 夢中で
		ENDIF
		PRINTL 腰を振っている……
	ENDIF
ENDIF
WAIT

;-------------------------------------------------
;後背位アナルさせる
;-------------------------------------------------
@MESSAGE_COM_後背位アナルさせる
PRINTFORM %CALLNAME:PLAYER%は
IF TFLAG:前コマンド実行者 == PLAYER
	SELECTCASE PREVCOMS
		CASE "正常位させる"
			IF 上級売春中
				PRINTFORML アナルセックスを求める%CALLNAME:TARGET%に従い四つん這いになると、剛直を抜かれて寂しくなった膣口を自分で弄りながら%CALLNAME:TARGET%を誘った……
			ELSE
				PRINTFORML 膣内に挿入して腰を振る%CALLNAME:TARGET%を制して四つん這いになり、アナルに挿れるように命じた……
			ENDIF
		CASE "後背位させる"
			IF 上級売春中
				PRINTFORML 膣内から剛直が引き抜かれたかと思うと尻穴に硬いものを当てられる感触がして、次第にそれは%CALLNAME:PLAYER%の中へと入り込んできた……
			ELSE
				PRINTFORML 後ろから膣内に突き入れる%CALLNAME:TARGET%に、今度はアナルで抱くように命じた……
			ENDIF
		CASE "正常位アナルさせる"
			PRINTFORML 尻穴に何度も突き入れる%CALLNAME:TARGET%を制して四つん這いになり、尻を振ってアナルセックスの続きを要求した……
		CASE "後背位アナルさせる"
			PRINTFORML 後ろからアナルを蹂躙される快感に\@ PENIS(PLAYER) ? 自分のペニスも膨らませながら # 太腿へ愛液を滴らせながら \@、%CALLNAME:TARGET%に性行為を続けさせた……
		CASEELSE
			GOTO ELSE
	ENDSELECT
ELSE
	$ELSE
	IF OVER_GENITAL(PLAYER) != ""
		IF CFLAG:PLAYER:挿入可
			SIF TEQUIP:PLAYER:下半身下着
				PRINTFORM %CSTR:PLAYER:下半身下着%を降ろして
		ELSE
			SIF TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着
				PRINTFORM %OVER_GENITAL(PLAYER)%
			SIF TEQUIP:PLAYER:下半身下着 && (TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着)
				PRINT と
			SIF TEQUIP:PLAYER:下半身下着
				PRINTFORM %CSTR:PLAYER:下半身下着%
			PRINT を脱いで
		ENDIF
	ENDIF
	IF TEQUIP:ゲームセンター
		PRINT 壁に手をつき
	ELSE
		PRINT 四つん這いになると、
	ENDIF
	PRINTFORM %CALLNAME:TARGET%の
	IF TALENT:アニマル
		PRINT ペニスの根本を掴み、
	ELSEIF !PENIS(TARGET)
		PRINT 股間から伸びるペニスバンドを
	ELSE
		SIF TALENT:童貞
			PRINT 女を知らない初心な
		PRINT ペニスを
	ENDIF
	SIF (TEQUIP:PLAYER:下半身上着 || TEQUIP:PLAYER:全身上着) && CFLAG:PLAYER:挿入可
		PRINTFORM %OVER_GENITAL(PLAYER)%の中から覗く
	PRINTL 自らのアナルに導いた……
ENDIF

IF 上級売春中
	IF TALENT:童貞
		PRINTFORML 童貞をアナルで捨てようとする物好きな%CALLNAME:TARGET%だが、きつい尻穴と緊張が重なり一苦労を要した……
	ELSE
		SELECTCASE SEX(PLAYER)
			CASE 1
				PRINTFORML %CALLNAME:TARGET%は激しく掘られて先走りを漏らす%CALLNAME:PLAYER%のペニスを手で扱き、精液を搾り出すように前立腺を責め立てている……
			CASE 2
				PRINTFORML %CALLNAME:TARGET%は尻穴を犯しながら秘所を弄り続け、%CALLNAME:PLAYER%は多様な快感に激しく身悶えしている……
		ENDSELECT
	ENDIF
ELSE
	IF TALENT:アニマル
		IF CFLAG:依存度 >= 1500
			PRINTFORML %CALLNAME:TARGET%は生温かい舌で%CALLNAME:PLAYER%の首や背中を舐めながら、交尾に夢中になっている……
		ELSE
			PRINTFORML %CALLNAME:TARGET%は本能のままに腰を振り続け、息を荒げている……
		ENDIF
	ELSEIF TALENT:童貞
		PRINTFORML 初めてで勝手の分からない%CALLNAME:TARGET%を、%CALLNAME:PLAYER%は尻を振って締め付けた……
	ELSEIF CFLAG:陥落
		PRINTFORM %CALLNAME:TARGET%は\@ FALLTYPE(TARGET) == 1 ? 愛する #  \@%CALLNAME:PLAYER%とのセックスに、
		IF TALENT:小柄体型 && TALENT:PLAYER:小柄体型
			PRINT 互いの幼い下半身をぶつけ合いながら
		ELSEIF TALENT:小柄体型 && !TALENT:PLAYER:小柄体型
			PRINTFORM 小さな体で必死に%CALLNAME:PLAYER%の体にしがみついて
		ELSEIF !TALENT:小柄体型 && TALENT:PLAYER:小柄体型
			PRINT ペニスを咥え込む幼い体を気遣いながら優しく
		ELSE
			SELECTCASE TALENT:バストサイズ
				CASE 1, 2
					PRINT 薄い胸を擦り付けるように覆いかぶさり
				CASE 3, 4, 5
					PRINT 乳房を押し付けながら
			ENDSELECT
			PRINT 夢中で
		ENDIF
		PRINTL 腰を振っている……
	ENDIF
ENDIF
WAIT

